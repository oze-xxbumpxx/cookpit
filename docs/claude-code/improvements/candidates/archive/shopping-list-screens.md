# 改善候補: shopping-list-screens（Sprint 4 Unit B）

> reflection-agent が起票。1 タスクの振り返りから抽出した知見をまとめる。
> 設定は変更しない。昇格判断は [../../memory-policy.md](../../memory-policy.md) の昇格条件に従う。

- **task-id**: shopping-list-screens
- **作成日**: 2026-07-14
- **対象タスク概要**: Sprint 4 Unit B（買い物リスト画面 + PWA オフライン強化）。L2・Presentation 層のみ
  （`apps/web`）。実装ルート: Orchestrator 経路（implementer）。2026-07-13（前セッション）に要件整理〜
  実装まで 1 セッションで完走し usage リミットで中断、2026-07-14（本セッション・別セッション）が
  `logs/` とリモートブランチのチェックポイントコミットから中断点を復元し、reviewer レビュー →
  Should 2 件の同日修正 → manual-browser-verify → 締めを消化。PR #59（2026-07-14 マージ）で完了。
- **関連成果物**: `docs/designs/shopping-list-screens.md` / `docs/implementation-plans/shopping-list-screens.md` /
  `docs/tests/shopping-list-screens.md` / `docs/05-roadmap.md`「Unit B 完了記録」/
  `logs/2026-07-13.md`（前セッション: 上流〜実装） / `logs/2026-07-14.md`（本セッション: レビュー〜締め）。
  `docs/reviews/shopping-list-screens.md` は**作成していない**（L2 のため
  `orchestration-policy.md` の運用フロー図・`definition-of-done.md` とも docs/reviews/ 記録は
  L3 限定＝L2 は「必要なら」の任意扱いで、今回はレビュー結果を `logs/2026-07-14.md` にのみ記録した。
  ポリシー上の欠落ではない — shopping-list-core（L3）事象 6 とは前提が異なるため区別して記載する）。

## 観測した事象（複数可）

### 事象 1: usage リミット強制中断からのセッション跨ぎ復旧が、日次ログ + リモートブランチの確認だけで機能した（成功パターン・複数タスク再現）

- **種類**: 成功手順
- **観測した事象**: 前セッション（2026-07-13）は要件整理〜画面設計〜実装計画〜試験計画〜実装まで
  1 セッションで完走し、品質ゲート green を確認した直後に usage リミットで強制終了した。成果は
  フェーズごとのチェックポイントコミット 3 本として `feature/shopping-list-screens` にプッシュ
  済みだった。本セッション（2026-07-14・別セッション）は `logs/2026-07-13.md` の内容と
  `git ls-remote` によるリモートブランチの状態確認だけで中断点を正確に復元し、残工程（reviewer
  レビュー → Should 修正 → manual-browser-verify → 締め）のみを過不足なく再開できた（二重作業・
  手戻りゼロ）。
- **発生回数**: 「日次ログ + Git 上の成果物確認だけでセッションを跨いだ引き継ぎが機能した」という
  広い括りでは累計 3 回目（test-infra-expansion の「計画セッション分離型」・shopping-list-core
  事象 9 の「次回やること」チェックリスト引き継ぎ・今回）。ただし今回は usage リミットによる
  **強制**中断からの復旧という点で前 2 件（計画的なセッション分割）とは性質が異なり、
  「チェックポイントコミット + `git ls-remote` によるリモート状態確認」という手段が新規に
  加わっている。この特定の手段としては累計 2 回目（ユーザー依頼文中の「前例: 7/11 ログの
  引き継ぎ成功」を含めれば）。
- **対象タスク**: shopping-list-screens（今回）。関連: test-infra-expansion / shopping-list-core
  （事象 9）/ 2026-07-11 ログ引き継ぎ（前例）。
- **原因仮説**: (a) 前セッションがフェーズ境界ごとにチェックポイントコミットを残す運用を徹底して
  いた、(b) 日次ログの「今日のタスク」「やったこと」「次回やること」形式が中断理由・完了フェーズ・
  残工程を機械的に判別可能な粒度で記録されていた、(c) 別セッションでもリモートブランチという
  「セッションを跨いでも消えない」情報源を確認する習慣が根付いていた。3 つが揃うことで
  session_id が変わっても文脈復元コストがほぼゼロになった。
- **改善案**: 「usage リミット等の強制中断が発生した場合、次セッションは (1) 直近の
  `logs/<date>.md` を読む (2) 対象 feature のリモートブランチを `git ls-remote` 等で確認する
  (3) 両者を突き合わせて未完了工程のみを再開する」という手順を、close-session Skill または
  development-workflow.md の「セッション跨ぎの復旧」節として明文化する価値がある。現状この手順は
  各セッションが経験的に再発明しており、明文化されたチェックリストがない。
- **変更対象**: （未定。候補: `docs/claude-code/development-workflow.md` または
  close-session 関連 Skill への「強制中断からの復旧手順」節の追加）
- **想定される副作用**: 手順を固定化しすぎると、実際の中断理由（usage リミット／分類器障害／
  ユーザー都合の中断）によって最適な復旧手段が異なる場合に窮屈になる可能性がある。「共通の
  復旧の型」と「中断理由ごとの分岐」を区別して書く必要がある。
- **評価方法**: 次回同様の強制中断（usage リミット等）が発生した際に、同じ手順（ログ + リモート
  ブランチ確認）で復旧できるか、かつ復旧までのやり取り往復数が今回と同等以下に収まるかを観察する。
- **昇格判定**: **満たす**（成功パターンの複数タスク再現。ただし個々の再現で使われた具体的手段
  ―計画的分割／ログチェックリスト／強制中断からのリモート確認―が微妙に異なるため、これらを
  1 つの手順としてどこまで一般化するかは agent-improvement-manager の横断分析に委ねる）。

### 事象 2: reviewer の Should 2 件は、設計書のエラー処理節の意図と実装のズレであり、実装計画・試験計画のいずれにも現れないタイプの指摘だった

- **種類**: レビュー指摘
- **観測した事象**: reviewer レビュー（Must 0・Should 2・Nice 5・マージ可）の Should 2 件は
  いずれも `shopping-list-client.tsx` の状態管理に関するもので、(S-1) refetch 成功時に既存の
  エラーバナーが残留する、(S-2) silent な focus refetch でも更新ボタンが一瞬 disable される、
  という設計書のエラー処理節が意図していた挙動と実装のズレだった。試験計画（117 ケース）にも
  実装計画にも、この 2 つの状態遷移パターン（「エラー表示中に成功したら消す」「silent refetch は
  UI をブロックしない」）を明示的に固定するテストケースが含まれておらず、reviewer の目視確認で
  初めて検出された。同日中に修正し回帰テスト LC-21 / LC-22 を追加（22 件 green）。
- **発生回数**: このタスク内 1 回（このタスクとしては初観測。過去の候補ファイルに同種の
  「エラー処理の状態遷移がテスト計画から漏れる」パターンの記録は無い）。
- **対象タスク**: shopping-list-screens
- **原因仮説**: 試験計画のエラー系観点（`docs/tests/shopping-list-screens.md`）はエラーの
  「発生」パターン（バリデーション失敗・API エラー）は網羅していたが、「エラーが解消される
  遷移」（成功後のクリア・silent 更新中の UI 状態）という**エラーからの復帰**方向の観点が
  手薄だった可能性がある。設計書のエラー処理節に書かれた意図（成功パスでのクリア等）を
  試験計画作成時に逐一テストケース化するチェックが無い。
- **改善案**: create-test-plan Skill のエラー系観点に「エラー表示から正常表示への復帰遷移
  （成功時のクリア・silent 更新中の UI 非ブロック）を、設計書のエラー処理節から個別にテスト
  ケース化する」観点を追加する候補。ただし発生 1 回のため即時の Skill 改訂は提案しない。
- **変更対象**: （未定。候補: `.claude/skills/create-test-plan/SKILL.md` のエラー系観点）
- **想定される副作用**: エラー系のテストケースが「発生」と「復帰」の両方向を要求されると
  ケース数が増える。すべてのエラー処理に復帰パターンのテストを義務付けると過剰になる懸念が
  あり、「楽観的更新・silent refetch を伴う状態管理コンポーネント」等、対象を絞る運用注記が
  必要になりそう。
- **評価方法**: 次回、楽観的更新・silent refetch を伴う画面のレビューで同種の Should
  （エラー処理の復帰遷移漏れ）が出るかを観察する。
- **昇格判定**: Memory 留め（昇格せず・単発。次回同種の指摘が出れば累計 2 回目として監視強化）。

### 事象 3: リモート実画面確認（dev:pglite + Playwright 自動化）が 2 タスク連続で成功し、新知見（Base UI SelectField の操作パターン）が蓄積した（成功パターン・複数タスク再現）

- **種類**: 成功手順 / 再利用可能な知見
- **観測した事象**: `pnpm --filter @cookpit/web dev:pglite` + Playwright での実画面確認が、
  MB-01〜09・MB-13〜15 の 12 項目すべて live PASS で完走した（BLOCKED は環境制約による PWA
  3 項目のみ）。加えて、Base UI の `SelectField`（店舗変更ドロップダウン等で使用）はネイティブ
  `<select>` ではなくトリガー button + popup 実装であり、Playwright では
  `getByLabel(ラベル).click()` → `getByRole('option', {name})` の操作パターンが必要という
  新規知見が判明した（ネイティブ select 前提の `selectOption()` は使えない）。
- **発生回数**: 「dev:pglite + Playwright スクリプト自動化による実画面確認の成功」としては
  累計 2 回目（1 回目: meal-plan-screens `docs/claude-code/improvements/candidates/meal-plan-screens.md`
  事象 3。つまずき所として searchbox ロール・Chromium executablePath 明示・スクリプト設置場所を
  記録済みだが、まだ manual-browser-verify Skill には未反映）。Base UI SelectField の操作
  パターンはこのタスクでの初観測。
- **対象タスク**: shopping-list-screens（今回）/ meal-plan-screens（前回）
- **原因仮説**: dev:pglite 経路の確立（`apps/web/scripts/setup-pglite-dev.mjs`）と、
  role/label ベースのセレクタ選定という一般化可能な方針が定着してきたことで、リモート環境でも
  実画面確認を自動化しやすくなっている。個別コンポーネント固有の落とし穴（今回は Base UI の
  SelectField）は都度発見され、Skill に反映されないまま各タスクの candidate ファイルに
  散在している。
- **改善案**: manual-browser-verify Skill に、(a) 一般化できる自動化の型（dev:pglite 起動 →
  role/label ベースのセレクタ → PASS/BLOCKED/FAIL の機械的出力）と、(b) 個別コンポーネントの
  操作パターン集（searchbox ロール・Base UI SelectField のトリガー+popup 操作・Chromium
  executablePath 明示・スクリプト設置場所）を反映する。2 タスク連続で有効だったため、
  memory-policy の「成功パターンが複数タスクで再現した」を満たす。
- **変更対象**: `.claude/skills/manual-browser-verify/SKILL.md`
- **想定される副作用**: 個別コンポーネントの操作パターンを列挙しすぎると Skill が肥大化する。
  「一般原則（role/label ベース）」と「既知の UI ライブラリ固有の例外（Base UI 等）」を分けて
  書き、後者は表形式で簡潔にまとめる運用が必要。
- **評価方法**: 次回の画面ユニットタスクで、同じ自動化スクリプト構成が再利用され、かつ
  新規の UI コンポーネント固有の落とし穴がある場合に Skill の記載だけで対処できるかを確認する。
- **昇格判定**: **満たす**（成功パターンの複数タスク再現。shopping-list-core 事象 1→4 と同様に
  2 タスクでの再現をもって昇格条件を満たすと判断。proposal 化は
  agent-improvement-manager による横断分析を推奨）。

### 事象 4: PWA オフライン確認（MB-10〜12）がリモート環境で原理的に BLOCKED になる具体的原因（本番ビルドの dead code 除去）が判明した

- **種類**: プロセス観察（環境制約の定型化検討）
- **観測した事象**: `apps/web/src/db/client.ts` は本番ビルド時に pglite 分岐を dead code
  として除去する設計（Neon 経路を保護する意図的な設計）であるため、リモート環境では
  「本番ビルド + DB あり」という PWA 確認に必要な組み合わせが原理的に成立しない。この
  BLOCKED 判定自体は manual-browser-verify Skill の「環境制約の扱い」節が既に一般論として
  想定済み（"Neon 固有機能・PWA/Service Worker 等" は BLOCKED の例として記載されている）。
  今回新たに判明したのは、その BLOCKED になる**具体的な技術的理由**（本番ビルドの dead code
  除去）であり、次回以降の同種確認で毎回コードを読んで理由を再構成する必要がなくなる。
- **発生回数**: このタスク内 1 回（BLOCKED という結論自体は Skill が既に一般論として想定
  済みのため「新規パターン」ではないが、具体的な技術的理由の言語化としては初観測）。
- **対象タスク**: shopping-list-screens
- **原因仮説**: manual-browser-verify Skill 執筆時点では PWA/Service Worker が
  「Neon 固有機能」と並ぶ BLOCKED の一例として抽象的に書かれており、リモート環境で
  具体的に何が原因で確認できないかまでは書かれていなかった。
- **改善案**: manual-browser-verify Skill の「環境制約の扱い」節に、PWA/Service Worker が
  BLOCKED になる具体的理由（本番ビルドは DB クライアントの環境分岐を dead code 除去するため、
  リモート環境で「本番ビルド + PGlite」の組み合わせが作れない）を定型文として追記しておくと、
  次回以降のタスクで同じコードリーディングを繰り返さずに済む。
- **変更対象**: `.claude/skills/manual-browser-verify/SKILL.md`（既存の「環境制約の扱い」節への
  追記。新規節ではない・軽微な変更）
- **想定される副作用**: なし（既存の一般論を具体化するだけで、判定基準自体は変えない）。
- **評価方法**: 次回 PWA/Service Worker 関連の実画面確認タスクで、コードリーディングによる
  理由確認の工程が Skill の記載だけで省略できるかを確認する。
- **昇格判定**: Memory 留め（昇格せず・単発だが低コストな追記候補。次回同種確認が発生した
  際に一緒に反映することを推奨）。

### 事象 5: 実装計画の「実装ルート」欄に判断理由が記録されず、後日ユーザーから「なぜ implementer 経路になったのか」という質問が発生した

- **種類**: 要件見落とし（成果物の記録漏れ）
- **観測した事象**: `docs/implementation-plans/shopping-list-screens.md` の冒頭は
  「実装ルート: Orchestrator 経路（implementer）」という結論のみが記録され、判断理由の一文が
  無かった。先例の `docs/implementation-plans/meal-plan-screens.md` は同じ位置に
  「2026-07-09 ユーザー判断で変更（ハーネス改善 IMP-009/010/012/014/016 の実地検証を兼ねる
  ため）」という理由を併記していた。この記録の欠落により、ユーザーから後日「どういう判断で
  implementer になったのか」という質問が発生し、`docs/06-ai-tools.md`「実装ルートの使い分け」
  の基準文書と先例（meal-plan-screens・store-master 等の実績表）から判断根拠を再構成して
  回答する追加の往復が生じた。
- **発生回数**: このタスク内 1 回（欠落そのものは初観測。同じ実装計画テンプレートを使う
  meal-plan-screens では理由が書かれていたため、「テンプレートに項目が無く、書くかどうかが
  作成者依存になっている」という構造がこの欠落の背景にある）。
- **対象タスク**: shopping-list-screens
- **原因仮説**: `create-implementation-plan` Skill のテンプレート（`実装ルート: L2 | L3` の
  行のみ）には実装ルート判断理由を書く欄が無く、必須項目としても定義されていない。
  meal-plan-screens で理由が書かれたのは「ユーザー判断による変更」という特殊事情があった
  ためで、ルート決定が既定路線どおり（Orchestrator 経路が通常）の場合は理由を省略しても
  テンプレート上は問題にならない。
- **改善案**: `create-implementation-plan` Skill のテンプレートに「実装ルート」欄と併せて
  「判断理由（1 行。既定どおりなら『既定』、変更・特殊判断があれば理由）」を必須項目として
  追加する。`docs/06-ai-tools.md` の判断の目安（指示書に書き切れるなら Codex／実装中に設計
  判断が発生しうるなら Orchestrator）のどの観点で判断したかを 1 行で残すだけでよく、
  コストは小さい。
- **変更対象**: `.claude/skills/create-implementation-plan/SKILL.md`（テンプレートへの
  項目追加）
- **想定される副作用**: 「既定どおり」のケースでも毎回 1 行書く手間が発生する。ただし
  1 行で済むため、実装計画自体の作成コストへの影響は軽微と見込む。
- **評価方法**: 次回の実装計画で「判断理由」欄が埋まっているか、かつ同種のユーザー質問
  （なぜこのルートか）が発生しないかを確認する。
- **昇格判定**: Memory 留め（昇格せず・発生 1 回）。ただし実害（ユーザーへの追加説明の
  往復発生）が既に 1 回生じており、原因が明確・修正が小さい（テンプレートへの 1 行追加）
  という性質は IMP-2026-012 の例外的早期昇格の前例と同型のため、次に同種の欠落・質問が
  1 回でも再発した場合は 3 回蓄積を待たずに早期昇格を検討する価値がある旨をここに明記する
  （採否は manager 判断）。

### 事象 6: E2E smoke の「リモート実行不可」という前提が dev:pglite の実証により緩和されたが、今回は見送りを維持した

- **種類**: 設計判断の前提変化（再利用可能な知見）
- **観測した事象**: 試験計画 `docs/tests/shopping-list-screens.md`「E2E smoke の要否判断」節は、
  先例の meal-plan-screens が「実行にはローカル DB が必要でリモート環境では緑判定できない」
  ことを理由に E2E 追加を推奨・任意（完了条件に含めない）とした判断をそのまま踏襲した。
  しかし本タスクの manual-browser-verify では `dev:pglite` + Playwright によりリモート環境
  でも DB ありの live 確認（実質的に E2E ハッピーパスに近い操作列）が可能であることが実証
  されており、「E2E はローカル DB 必須・リモート実行不可」という前提そのものは緩和されつつ
  ある。今回はこの点をユーザーへ確認したうえで、スコープ超過を避けるため見送りの判断を
  維持した（新規の Playwright テストファイル追加は行わない判断）。
- **発生回数**: このタスク内 1 回（前提の変化に気づいた初観測。実害・手戻りは無し）。
- **対象タスク**: shopping-list-screens。関連: meal-plan-screens（先例の判断根拠）
- **原因仮説**: `docs/tests/<feature>.md` の作成時に先行事例の判断をそのまま踏襲する運用が
  効率的である一方、判断根拠となった技術的前提（当時「ローカル DB 必須」だった）が別の
  取り組み（manual-browser-verify の dev:pglite 自動化）によって陳腐化していないかを
  都度再確認する仕組みが無い。
- **改善案**: 特に Skill/Rule の変更は提案しない。次の画面ユニットで E2E smoke の要否を
  判断する際に、「dev:pglite + Playwright による自動化がどこまで E2E の代替になるか」を
  再評価材料として引き継ぐ。
- **変更対象**: なし（次回タスクへの申し送り事項として記録するのみ）
- **想定される副作用**: なし
- **評価方法**: 次の画面ユニットで E2E smoke 要否を判断する際に、この前提変化を踏まえた
  再評価が行われるかを確認する。
- **昇格判定**: Memory 留め（昇格せず・単発。次の画面ユニットでの再評価材料として
  improvement-backlog.md の申し送り表で追跡する）。

### 事象 7: PR 集約先の作業ブランチが二重化し、ユーザー確認が必要になった（usage リミット中断→別セッション再開に固有の可能性）

- **種類**: プロセス観察（Agent 間・セッション間の運用の曖昧さ）
- **観測した事象**: 前セッション（2026-07-13）の成果は `feature/shopping-list-screens` に
  プッシュされていたが、本セッション（2026-07-14）はセッション指定のブランチ
  `claude/b-case-screen-design-ttnu9f` で作業を継続した（Should 修正・docs 同期・本ログ作成）。
  結果として同一 feature に対して 2 本のブランチが並存し、PR #59 をどちらへ集約するかの
  確認がユーザーとの間で必要になった（最終的にユーザー確認で解消し、実害は無かった）。
- **発生回数**: このタスク内 1 回。同種の暗黙のブランチ分岐は他 feature でも起きている
  可能性があるが、本セッションでは他タスクでの再現は確認できていない（不明のまま記録する）。
  shopping-list-core 事象 7（受け入れレビュー合格後の PR 作成・マージ主体の暗黙受け渡し）とは
  別種の事象だが、いずれも「セッション・工程の境界をまたぐ際の受け渡しルールが明文化されて
  いない」という同じ系統の観察である。
- **対象タスク**: shopping-list-screens
- **原因仮説**: セッションが変わると Claude Code 側がセッション固有のブランチ名
  （`claude/<slug>`）を新規に採番する挙動と、前セッションが独自に作成した `feature/*`
  ブランチが併存しうる構造にある。usage リミットによる強制中断 → 別セッションでの再開、
  という特殊なシナリオでのみ顕在化した可能性が高く、通常の計画的なセッション分割
  （test-infra-expansion 等）では単一ブランチで完結していたため発生していない。
- **改善案**: 特に Skill/Rule の変更は提案しない。強制中断からの再開時に「前セッションの
  成果がどのブランチにあるか」を確認し、同一ブランチで継続するか明示的に判断する一手間を、
  事象 1 で提案した「強制中断からの復旧手順」に含めることを検討する（事象 1 と統合して
  manager が判断する余地を残す）。
- **変更対象**: なし（事象 1 の改善案に統合検討）
- **想定される副作用**: なし
- **評価方法**: 次回、usage リミット等による強制中断→別セッション再開が発生した際に、同様の
  ブランチ二重化が起きるかを観察する。
- **昇格判定**: Memory 留め（昇格せず・単発。強制中断からの再開という特殊シナリオに固有か、
  一般的に繰り返すかは再発を待って判断する）。

## メトリクス記録

`.claude/state/subagent-log.jsonl` には本タスクに対応するエントリが無く（2026-07-13 の
`feature: null` の 1 件のみが残存しており本タスクとは無関係）、`record-task-metrics.sh` の
実行には Bash ツール権限が必要（本振り返りセッションは Read/Write/Grep/Glob のみ）。
`docs/claude-code/improvements/metrics/` に shopping-list-screens 用の YAML は作成していない。
次回 Bash 権限を持つセッションで作成・補完することを推奨する（shopping-list-core 振り返り時と
同じ制約）。IMP-2026-007 の追跡計測は L3 タスク限定のため、本タスク（L2）では対象外。

## まとめ

- 改善候補として起票したもの（→ backlog に追記した行）:
  - 事象 1（usage リミット強制中断からのセッション跨ぎ復旧・複数タスク再現）
  - 事象 3（dev:pglite + Playwright 実画面確認自動化・複数タスク再現。Base UI SelectField 知見含む）
- Memory に留めたもの（昇格せず・再発監視）:
  - 事象 2（reviewer Should 2 件・エラー処理の復帰遷移がテスト計画から漏れる。単発）
  - 事象 4（PWA BLOCKED の具体的技術的理由の言語化。Skill 既存項目への軽微な追記候補）
  - 事象 5（実装ルート判断理由の記録漏れ・ユーザー質問を誘発。実害 1 回・早期昇格の可能性を明記）
  - 事象 6（E2E smoke「リモート実行不可」前提の緩和。次の画面ユニットでの再評価材料）
  - 事象 7（PR ブランチの二重化。強制中断→別セッション再開に固有の可能性、事象 1 と統合検討）
