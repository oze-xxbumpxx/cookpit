# 改善候補: TASK-2026-009

> 本タスクの振り返り。設定変更そのものはユーザー承認済みの `review-readiness` 設計に基づく。
> ここでは追加の恒久ルールを自動昇格させず、観測事実を残す。

- **task-id**: TASK-2026-009
- **作成日**: 2026-08-11
- **対象タスク概要**: 人間レビューの認知負荷を下げる review-readiness 基盤（L3）
- **関連成果物**: `docs/requirements/review-readiness.md` /
  `docs/designs/review-readiness.md` / `docs/reviews/review-readiness.md`

## 観測した事象

### 事象 1: レビュー対象の鮮度にはファイル名でなく内容識別子が必要

- **種類**: Agent 間認識不一致 / 設計手戻り
- **観測した事象**: architecture-designer の初期案に、ファイル名中心の対象識別と固定的な
  人間確認が含まれた。内容だけ変わる反例と、人間項目が増えたときの隠蔽リスクを提示し、
  base + raw diff metadata の digest と「4 件以上は引き渡さない」設計へ改めた。
- **発生回数**: このタスク内 1 回
- **対象タスク**: TASK-2026-009
- **原因仮説**: 監査ログの識別と意思決定 UI の簡潔さを同時に最適化すると、安全性が暗黙になりやすい。
- **改善案**: 設計レビュー時に「同じ path で内容変更」「対象変更後の stale」「budget 超過」の
  3 反例を先に当てる。
- **変更対象**: 設計 / 試験計画
- **想定される副作用**: 小変更にも digest 設計を要求すると過剰。review workflow 変更時だけ適用する。
- **評価方法**: `.claude/tests/review-readiness.test.mjs` の digest / budget 回帰試験。
- **昇格判定**: Memory 留め（本 feature の設計と試験へ反映済み、単発）。

### 事象 2: CI が review 文書だけを見ると「欠落」を検出できない

- **種類**: レビュー指摘 / テスト失敗前の自己検出
- **観測した事象**: 最初の `ci` 実装は変更された `docs/reviews/*.md` だけを列挙し、review を
  作らなかった feature を「対象なし」にしていた。L2/L3 成果物の basename から候補を得る実装と
  missing review の warn/strict 試験を追加した。
- **発生回数**: このタスク内 1 回
- **対象タスク**: TASK-2026-009
- **原因仮説**: 存在する成果物の妥当性検査と、必要な成果物の存在検査を同じものと捉えた。
- **改善案**: checker の試験に「入力成果物そのものが無い」negative control を必ず 1 件置く。
- **変更対象**: Script / Test
- **想定される副作用**: 歴史文書の編集にも警告が出うるため、初期は warning-only を維持する。
- **評価方法**: CI-07 と、次の 2 feature 以上で false warning を計測する。
- **昇格判定**: Memory 留め（実装・試験へ反映済み、再発監視）。

### 事象 3: 配布対象の実数検算に個人設定が混入した

- **種類**: 文書整合性 / 成功手順
- **観測した事象**: `find .claude -type f` の実数が想定 74 に対して 75 となり、global ignore の
  `.claude/settings.local.json` が混入していると判明した。検算コマンドで個人設定を明示除外した。
- **発生回数**: このタスク内 1 回
- **対象タスク**: TASK-2026-009
- **原因仮説**: filesystem inventory と配布対象 inventory の境界がコマンドに表現されていなかった。
- **改善案**: 配布対象の検算は ignore 済み personal state を除外し、表の合計とも突き合わせる。
- **変更対象**: Documentation
- **想定される副作用**: 除外対象を増やすと本当の未分類を隠せるため、名前を完全一致で限定する。
- **評価方法**: manifest 記載コマンドが 74、ディレクトリ表の合計が 74、未分類 0 で一致すること。
- **昇格判定**: Memory 留め（manifest 内で解消済み、単発）。

### 事象 4: review 後の metrics 更新が subject を stale にする

- **種類**: 設計手戻り / dogfood
- **観測した事象**: 初回 Reviewer / Security Reviewer 後に review 件数を metrics へ記録すると、
  metrics は digest 対象なので subject が変化した。review 文書以外を広く除外せず、metrics と
  candidate を確定してから final subject を closure review する順序へ改めた。
- **発生回数**: このタスク内 1 回
- **対象タスク**: TASK-2026-009
- **原因仮説**: review の自己参照は考慮したが、review 結果を入力にする周辺 audit 成果物の
  時系列が設計に明記されていなかった。
- **改善案**: L3 は preliminary review → audit 成果物確定 → final subject → closure review →
  review 文書生成の順で閉じる。
- **変更対象**: Design / Workflow / Skill
- **想定される副作用**: AI review が 1 回増えうる。closure は前回からの差分中心にし、
  人間 gate は増やさない。
- **評価方法**: closure 後に `review-readiness.mjs check` が current を返すこと。
- **昇格判定**: Memory 留め（本 feature の正典へ反映済み、次の 2 feature で再発監視）。

### 事象 5: formatter が生成 packet を変更して render drift を起こす

- **種類**: dogfood / テスト失敗前の自己検出
- **観測した事象**: renderer の表と長文は正規出力でも、repository の Prettier が再整形すると
  hidden state からの再生成結果と一致しなくなる。marker 内へ Prettier range-ignore を含めた。
- **発生回数**: このタスク内 1 回
- **対象タスク**: TASK-2026-009
- **原因仮説**: renderer 単体の canonical 性だけを考え、後続 formatter を状態遷移へ含めていなかった。
- **改善案**: 生成領域を formatter から保護し、ignore marker 自体も parser の canonical 比較対象にする。
- **変更対象**: Script / Test / Documentation
- **想定される副作用**: packet 内の可読整形は renderer が全責任を持つ。formatter による自動改善は効かない。
- **評価方法**: `pnpm format:check` と U-06、実 review 文書の `check` が同時に成功すること。
- **昇格判定**: Memory 留め（本 feature で修正済み、単発）。

## まとめ

- 改善候補として起票したもの: なし（すべて本 feature 内で解消済み）。
- Memory に留めたもの: subject の反例設計、missing artifact の negative control、配布対象の
  明示除外、audit 成果物を先に確定する closure 順序、formatter 安定性。
