# タスク品質メトリクス（§19）
# 機密情報・会話全文は保存しない。自動取得できない数値は推測せず unknown とする。
task_id: ci-minutes-reduction
feature_name: ci-minutes-reduction
change_level: 2
date: 2026-07-25

agents:
  # 本タスクでは Subagent を使わず Orchestrator が直接実装した。
  # machine.totals.agent_calls=1 は同一セッションの前半（project-refactoring）の
  # reviewer 呼び出しを含む集計であり、本タスクの値ではない。
  calls: 0
  retries: 0
  unnecessary_calls: 0

# Codex 委譲ルートのタスクのみ記入（Orchestrator ルートは unknown のまま）。
# IMP-2026-025 の効果実測をモデル条件込みで比較するための記録（2026-07-18 追加）。
codex:
  model: unknown # 例: gpt-5-codex（実際に使ったモデル。ブリーフ README の確定値表と一致させる）
  reasoning_effort: unknown # 例: low | medium | high（Task/層で変えた場合は notes に内訳）
  notes: ''

quality:
  # CI 設定のみの変更のため Vitest の追加は無し。検証は静的検証・判定ロジックの
  # パターン検証（10 件）・実履歴 25 マージで代替（docs/tests/ci-minutes-reduction.md）。
  tests_added: 0
  initial_test_failures: 0
  reviewer_critical: 0
  reviewer_major: 0
  reviewer_minor: 0
  # ユーザーからの訂正は無し。ただし Orchestrator 側の自己訂正が 2 件あった
  # （削減率の過大提示。candidates/ci-minutes-reduction.md 事象 1）。
  user_corrections: 0
  self_corrections: 2
  escaped_defects: 0

process:
  requirement_rework: 0
  # 実測により削減案の見立てが変わった（静的チェック統合のみ→全ゲート統合）。
  design_rework: 1
  # 判定の正規表現の境界指定ミスを適用前に検出・修正（事象 2）。
  implementation_rework: 1
  # 実装計画・試験計画を独断で設計書に内包し、Stop hook の指摘で分離した（事象 3）。
  missing_documents: 2
  # Actions 課金停止のため CI 上での実地確認（試験計画 D-1〜D-6）が未実施。
  unresolved_items: 1

machine:
  # collect-task-metrics.mjs による自動集計。手で編集しない（--write の再実行で更新）
  collected_at: 2026-07-25
  branch_filters: ['ci-minutes-reduction']
  sessions: # session_id 単位でマージ・累積（1 行 = 1 セッション）
    - {
        'id': '65eedc1b-8ff6-54e3-a354-8928cd8c510b',
        'date': '2026-07-25',
        'branches': ['claude/ci-minutes-reduction', 'claude/project-refactoring-vc1pmz'],
        'matched_by': 'branch',
        'active_minutes': 172,
        'tokens':
          { 'input': 625, 'output': 277588, 'cache_creation': 1803822, 'cache_read': 94389192 },
        'tool_uses': 345,
        'user_prompts': 12,
        'agent_calls': { 'reviewer': 1 },
        'models': ['claude-opus-4-8', 'claude-opus-5'],
      }
  gate_history: # run-quality-gates.sh の実行記録（ts 単位でマージ・累積）
    []
  totals:
    sessions: 1
    active_minutes: 172 # 約2時間52分（活動時間ベース・空白30分超は除外）
    tokens_input: 625
    tokens_output: 277588
    tokens_cache_creation: 1803822
    tokens_cache_read: 94389192
    tokens_total: 96471227
    tool_uses: 345
    user_prompts: 12
    agent_calls: 1
    agent_calls_by_type: { 'reviewer': 1 }
    agent_restarts: 0 # 同一 Subagent 種の再起動数（手戻りプロキシ）
    gate_runs: 0
    gate_fail_runs: 0
    gate_rework_rate: unknown # ゲート実行記録なし
# 任意: before/after や IMP 定量比較でフェーズを分けたいときだけ埋める。
# 機械自動では埋めない。不明な欄は unknown のまま。
phases:
  requirements: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  design: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  planning: { tool_uses: unknown, tokens_total: unknown, notes: '' } # planner + test-designer
  implementation: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  review: { tool_uses: unknown, tokens_total: unknown, notes: '' }
