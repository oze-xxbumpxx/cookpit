# タスク品質メトリクス（§19）
# 機密情報・会話全文は保存しない。自動取得できない数値は推測せず unknown とする。
task_id: shopping-item-remove
feature_name: shopping-item-remove
change_level: 3
date: 2026-07-25

agents:
  # 本タスクは Explore 3 / Plan 1 を実装前の調査・設計に使い、実装以降は
  # メインセッションが直接行った（reviewer は起動していない）。machine.totals は
  # 同一ブランチの recipe-form-usability と合算値である点に注意。
  calls: 4
  retries: 0
  unnecessary_calls: 0

# Codex 委譲ルートのタスクのみ記入（Orchestrator ルートは unknown のまま）。
# IMP-2026-025 の効果実測をモデル条件込みで比較するための記録（2026-07-18 追加）。
codex:
  model: unknown # 例: gpt-5-codex（実際に使ったモデル。ブリーフ README の確定値表と一致させる）
  reasoning_effort: unknown # 例: low | medium | high（Task/層で変えた場合は notes に内訳）
  notes: ''

quality:
  # domain 10 / application 8 / infrastructure 2 / route 7 / 純関数 5 / 画面 17（行 3・group 1 含む）
  tests_added: 49
  # 画面テスト 1 件が初回 FAIL（「献立の変更を反映」がボタンラベルと衝突し getByText が複数一致）。
  initial_test_failures: 1
  # reviewer サブエージェントは起動しておらず、自己レビュー（docs/reviews/shopping-item-remove.md）。
  # Should 2 件・Nice 2 件を記録したが Must は無し。第三者レビュー未実施は同記録に明記。
  reviewer_critical: 0
  reviewer_major: 0
  reviewer_minor: 4
  # ユーザーからの訂正なし。ただし L3 完了条件の 2 点（reviews / candidates）の欠落を
  # ユーザーの質問で発覚 → process.missing_documents に計上。
  user_corrections: 0
  self_corrections: 0
  escaped_defects: 0

process:
  requirement_rework: 0
  design_rework: 0
  # 試験計画の SDL-12 / SDL-15 が実装と噛み合わず（楽観削除で行が消えるため観点が成立しない）、
  # 実装に合わせて観点を修正した。
  implementation_rework: 1
  # docs/reviews/shopping-item-remove.md と improvements/candidates/ の 2 点が
  # definition-of-done.md の L3 完了条件にありながら未作成だった（candidates 事象 1）。
  missing_documents: 2
  unresolved_items: 0

machine:
  # collect-task-metrics.mjs による自動集計。手で編集しない（--write の再実行で更新）
  collected_at: 2026-07-26
  branch_filters: ['shopping-list-improvements']
  sessions: # session_id 単位でマージ・累積（1 行 = 1 セッション）
    - {
        'id': 'f424aa45-2546-52ae-9d23-59fd7028921c',
        'date': '2026-07-25',
        'branches': ['claude/shopping-list-improvements-6q0o56'],
        'matched_by': 'branch',
        'active_minutes': 167,
        'tokens':
          { 'input': 551, 'output': 215762, 'cache_creation': 1823841, 'cache_read': 80361146 },
        'tool_uses': 349,
        'user_prompts': 14,
        'agent_calls': { 'Explore': 3, 'Plan': 1 },
        'models': ['claude-opus-5'],
      }
  gate_history: # run-quality-gates.sh の実行記録（ts 単位でマージ・累積）
    []
  totals:
    sessions: 1
    active_minutes: 167 # 約2時間47分（活動時間ベース・空白30分超は除外）
    tokens_input: 551
    tokens_output: 215762
    tokens_cache_creation: 1823841
    tokens_cache_read: 80361146
    tokens_total: 82401300
    tool_uses: 349
    user_prompts: 14
    agent_calls: 4
    agent_calls_by_type: { 'Explore': 3, 'Plan': 1 }
    agent_restarts: 2 # 同一 Subagent 種の再起動数（手戻りプロキシ）
    gate_runs: 0
    gate_fail_runs: 0
    gate_rework_rate: unknown # ゲート実行記録なし
# 任意: before/after や IMP 定量比較でフェーズを分けたいときだけ埋める。
# 機械自動では埋めない。不明な欄は unknown のまま。
phases:
  requirements: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  design: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  planning: { tool_uses: unknown, tokens_total: unknown, notes: '' } # planner + test-designer
  implementation: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  review: { tool_uses: unknown, tokens_total: unknown, notes: '' }
