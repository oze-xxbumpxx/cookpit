# タスク品質メトリクス（§19）
# 機密情報・会話全文は保存しない。自動取得できない数値は推測せず unknown とする。
task_id: shopping-list-price-comparison
feature_name: shopping-list-price-comparison
change_level: L2
date: 2026-08-05

agents:
  # レビュー対応フェーズ（セッション 5）はメインセッションが直接実施し Subagent 0 件。
  # 実装フェーズ（セッション 4・別ブランチ）の値はリモート環境で subagent-log が
  # セッションを跨がないため取得不能（IMP-2026-019 の既知事象）。
  calls: unknown
  retries: unknown
  unnecessary_calls: unknown

# Codex 委譲ルートのタスクのみ記入（Orchestrator ルートは unknown のまま）。
# IMP-2026-025 の効果実測をモデル条件込みで比較するための記録（2026-07-18 追加）。
codex:
  model: unknown # 例: gpt-5-codex（実際に使ったモデル。ブリーフ README の確定値表と一致させる）
  reasoning_effort: unknown # 例: low | medium | high（Task/層で変えた場合は notes に内訳）
  notes: ''

quality:
  # PR 全体で 67 件（it ブロック数）。うちレビュー対応分は 8 件（web 700 → 708）。
  tests_added: 67
  initial_test_failures: 0 # 実装フェーズ完了時点で 700 件 green
  reviewer_critical: 1 # Must: M-1（初期値なし reduce で画面全体が落ちる）
  reviewer_major: 7 # Should: S-1〜S-7
  reviewer_minor: 8 # Nice: N-1〜N-8
  user_corrections: 0 # ユーザー訂正なし（S-1/S-2/S-3/S-5 は提案 → 確認で得た判断）
  escaped_defects: 0

process:
  requirement_rework: 0
  design_rework: 2 # レビュー起因の設計変更（S-1 表示仕様 / S-3 基準区分の決め方）
  implementation_rework: 1 # MB-04 を前提を満たさないデータで実施し再実施
  missing_documents: 0
  unresolved_items: 7 # N-2〜N-8 を申し送り（今スプリント対応外と明記済み）
  # セッション時間に占めるメタ作業（改善サイクル・棚卸し・承認待ち・ハーネス議論）の割合。
  # 0.0〜1.0 または unknown。個人開発ライトモードの肥大監視用（IMP-2026-030）。
  meta_work_ratio: 0.13 # 活動 120 分中、振り返り・メトリクス記録が約 15 分

machine:
  # collect-task-metrics.mjs による自動集計。手で編集しない（--write の再実行で更新）
  collected_at: 2026-08-05
  branch_filters: ['shopping-list-price-comparison']
  # 注: 下記 sessions はレビュー対応セッションのみ。実装フェーズ（2026-08-04 セッション 4、
  # ブランチ feat/shopping-list-price-comparison）は別セッションのため集計に含まれない。
  sessions: # session_id 単位でマージ・累積（1 行 = 1 セッション）
    - {
        'id': 'e6896cbe-5b5a-5f53-acef-040e4f1f3ecd',
        'date': '2026-08-05',
        'branches': ['claude/shopping-list-price-comparison-mzsz9z'],
        'matched_by': 'branch',
        'active_minutes': 120,
        'tokens':
          { 'input': 7983, 'output': 94123, 'cache_creation': 537722, 'cache_read': 29555122 },
        'tool_uses': 177,
        'user_prompts': 12,
        'agent_calls': {},
        'models': ['claude-opus-5'],
      }
  gate_history: # run-quality-gates.sh の実行記録（ts 単位でマージ・累積）
    []
  totals:
    sessions: 1
    active_minutes: 120 # 約2時間（活動時間ベース・空白30分超は除外）
    tokens_input: 7983
    tokens_output: 94123
    tokens_cache_creation: 537722
    tokens_cache_read: 29555122
    tokens_total: 30194950
    tool_uses: 177
    user_prompts: 12
    agent_calls: 0
    agent_calls_by_type: {}
    agent_restarts: 0 # 同一 Subagent 種の再起動数（手戻りプロキシ）
    gate_runs: 0
    gate_fail_runs: 0
    gate_rework_rate: unknown # ゲート実行記録なし
# 任意: before/after や IMP 定量比較でフェーズを分けたいときだけ埋める。
# 機械自動では埋めない。不明な欄は unknown のまま。
phases:
  requirements: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  design: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  planning: { tool_uses: unknown, tokens_total: unknown, notes: '' } # planner + test-designer
  implementation: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  review: { tool_uses: unknown, tokens_total: unknown, notes: '' }
