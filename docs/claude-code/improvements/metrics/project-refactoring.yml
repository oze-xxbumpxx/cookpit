# タスク品質メトリクス（§19）
# 機密情報・会話全文は保存しない。自動取得できない数値は推測せず unknown とする。
task_id: project-refactoring
feature_name: project-refactoring
change_level: 2
date: 2026-07-25

agents:
  # subagent-log の自動集計は 6 だったが、machine.totals.agent_calls（=1）および会話上の
  # 実績と一致しない（過去セッションの行を拾ったと推測）。実際の呼び出しは reviewer 1 回のみ。
  calls: 1
  retries: 0 # 再依頼なし
  unnecessary_calls: 0

# Codex 委譲ルートのタスクのみ記入（Orchestrator ルートは unknown のまま）。
# IMP-2026-025 の効果実測をモデル条件込みで比較するための記録（2026-07-18 追加）。
codex:
  model: unknown # 例: gpt-5-codex（実際に使ったモデル。ブリーフ README の確定値表と一致させる）
  reasoning_effort: unknown # 例: low | medium | high（Task/層で変えた場合は notes に内訳）
  notes: ''

quality:
  # use-api-action のフック単体 7 件 + S1 回帰テスト UAA-08 の 1 件。
  # 観点 G の分割はテスト件数を変えていない（42/26/58 が完全一致）。
  tests_added: 8
  # 観点 D で meal-plan のモックが json() を持たず 4 件失敗（onSuccessWithoutBody 分離で解決）。
  initial_test_failures: 4
  reviewer_critical: 0
  reviewer_major: 0 # Must 指摘 0
  reviewer_minor: 2 # Should 2（S1 は修正 / S2 はユーザー判断で受容）
  reviewer_nits: 4 # Nice 4（N1 は S1 修正で同時解消 / N4 は対応済み）
  user_corrections: 0
  escaped_defects: 0

process:
  requirement_rework: 0
  # 観点 C: 「11 具象クラスすべてが 2 基底を継承」という設計書の前提が実装時に 1 件外れ、
  # 中間基底 InvalidOperationError を挟む設計に変更した。
  design_rework: 1
  # (1) 観点 A の機械置換で正規表現が隣接 import を巻き込み 70 ファイルを巻き戻して再実行。
  # (2) 観点 D で json() の呼び分けを onSuccess / onSuccessWithoutBody に分離。
  implementation_rework: 2
  missing_documents: 0
  # CI が緑になっていない（Dependency Audit の推移的依存 high 3 件。分岐元 main から既存）。
  unresolved_items: 1

machine:
  # collect-task-metrics.mjs による自動集計。手で編集しない（--write の再実行で更新）
  collected_at: 2026-07-25
  branch_filters: ['project-refactoring']
  sessions: # session_id 単位でマージ・累積（1 行 = 1 セッション）
    - {
        'id': '65eedc1b-8ff6-54e3-a354-8928cd8c510b',
        'date': '2026-07-25',
        'branches': ['claude/project-refactoring-vc1pmz'],
        'matched_by': 'branch',
        'active_minutes': 90,
        'tokens':
          { 'input': 549, 'output': 209140, 'cache_creation': 593620, 'cache_read': 72883438 },
        'tool_uses': 309,
        'user_prompts': 8,
        'agent_calls': { 'reviewer': 1 },
        'models': ['claude-opus-4-8', 'claude-opus-5'],
      }
  gate_history: # run-quality-gates.sh の実行記録（ts 単位でマージ・累積）
    []
  totals:
    sessions: 1
    active_minutes: 90 # 約1時間30分（活動時間ベース・空白30分超は除外）
    tokens_input: 549
    tokens_output: 209140
    tokens_cache_creation: 593620
    tokens_cache_read: 72883438
    tokens_total: 73686747
    tool_uses: 309
    user_prompts: 8
    agent_calls: 1
    agent_calls_by_type: { 'reviewer': 1 }
    agent_restarts: 0 # 同一 Subagent 種の再起動数（手戻りプロキシ）
    gate_runs: 0
    gate_fail_runs: 0
    gate_rework_rate: unknown # ゲート実行記録なし
# 任意: before/after や IMP 定量比較でフェーズを分けたいときだけ埋める。
# 機械自動では埋めない。不明な欄は unknown のまま。
phases:
  requirements: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  design: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  planning: { tool_uses: unknown, tokens_total: unknown, notes: '' } # planner + test-designer
  implementation: { tool_uses: unknown, tokens_total: unknown, notes: '' }
  review: { tool_uses: unknown, tokens_total: unknown, notes: '' }
