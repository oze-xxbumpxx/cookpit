# IMP-2026-028: Orchestrator 経路の実態への正典化と stop/resume 機構の凍結

> agent-improvement-manager 相当の横断分析（harness-complexity-audit）から起票。
> 重要設定は本適用せず差分を残す。承認境界は
> [../../improvement-cycle.md](../../improvement-cycle.md) §承認境界 を参照。

- **ステータス**: proposal
- **対象 Agent または Skill**: docs/claude-code/orchestration-policy.md /
  .claude/agents/orchestrator.md / docs/claude-code/development-workflow.md（同期）
- **承認区分**: 人間承認必須（Agent の責務・運用方針の変更）
- **起票日**: 2026-07-20
- **元候補**: candidates/harness-complexity-audit.md（事象 2）+ candidates/store-master.md（事象 1）+
  candidates/meal-plan-screens.md（事象 4）

## 背景

多段 background 委譲（Orchestrator を子エージェントとして経由する形を含む）が実運用で
安定せず、「子 Agent の成果物なし完了」→ メインセッションによる Orchestrator 役の代行、
というパターンが反復している。一方、この不安定さへの補強として積まれた stop/resume・
`inflight-agents.json`・冪等再開の機構（IMP-2026-007/008/009）が orchestration-policy 肥大
（352 行）の主因になっている。実際に安定稼働している型は
**「メインセッションが Orchestrator 役を務め、専門 Agent を直接起動。実装は Codex 委譲または
implementer」**であり、正典をこの実態に合わせて縮約する。

## 観測事象（根拠）

| 事象 | 再現タスク | 累計回数 | 昇格条件 |
| ---- | ---------- | -------- | -------- |
| stop/resume 後の notification 待ちループ | store-master（事象 1・2026-06-28） | 1 | 同系統 3 回以上 |
| 単一委譲でも background 化・同期待機前提の崩れ | meal-plan-screens（事象 4・2026-07-09） | 2 | 同上 |
| 多段 background 委譲で「成果物なし完了」×2 → メインが代行へ切替 | pantry-screens（logs/2026-07-19 セッション 2） | 3 | **充足** |

## 原因

恒久的・構造的。ハーネス側の子エージェント通知配送・background 挙動（orchestration-policy
§既知の制約に記録済み）は Cookpit の Agent 定義では修正できない。これに機構追加で対抗した
結果、最も複雑な部品（多段委譲 + stop/resume 機構）が最も使われない状態になった。

## 変更案

**追加ではなく削除・移動。** 3 点で構成する。

1. **正規ルートの明記**: 「Orchestrator 役はメインセッションが務める」を正典化する。
   - ローカル: `claude --agent orchestrator`（従来どおり・メインセッション起動）。
   - リモート: 通常セッションが orchestration-policy に従い専門 Agent を直接起動する
     （2026-07-19 に実際に機能した型）。
   - **Orchestrator 自体を Agent ツールの子として起動する多段委譲は行わない**
     （現行「既知の制約」の運用注記を禁止事項へ昇格）。
2. **stop/resume 機構の凍結**: §stop/resume を跨ぐ委譲の扱い（約 23 行）と既知の制約 3 節
   （約 41 行）を `docs/claude-code/archive/orchestration-frozen-mechanisms.md` へ**移動**
   （削除しない。ハーネス側の挙動が改善されたら復元判断）。`inflight-agents.json` の運用は停止。
   本文には次の 1 原則だけを残す:
   > **resume・再開直後は notification を待たず、期待成果物の存在・更新時刻で完了を冪等判定
   > してから次を決める（無ければ再委譲・二重起動しない）。**
   （IMP-2026-009 の一般化。この原則単体で機能した実績: meal-plan-screens 2026-07-09 /
   pantry-screens 2026-07-19）
3. **orchestrator.md の縮約**: 進め方 3 の `inflight-agents.json` 分岐（約 8 行）と
   進め方 4 の resume 分岐詳細（約 10 行）を、上記 1 原則への参照 2 行へ置換。

削減見込み: orchestration-policy 約 60 行（352 → 約 290 行）+ orchestrator.md 約 16 行。
委譲フロー・並列指針・各 Agent 起動条件・モデル采配は**変更しない**。

## 変更差分

```diff
--- docs/claude-code/orchestration-policy.md
-## stop/resume を跨ぐ委譲の扱い（通知非依存）
-（L122-144: inflight-agents.json の追記・クリア・L1/L2 拡張の全 23 行）
-### 既知の制約: 単一 Sub-agent 委譲でも background 起動になり得る（L145-153）
-### 既知の制約: Orchestrator を Agent ツールで子エージェントとして起動した場合（L154-171）
-### 既知の制約: フォアグラウンド割り込みによるバックグラウンドタスクの停止（L172-185）
+## 再開時の完了判定（1 原則）
+
+resume・再開直後は notification を待たず、直前に委譲した Sub-agent の期待成果物の
+存在・更新時刻で完了を冪等判定してから次を決める。存在すれば完了として次工程へ、
+無ければそのときだけ再委譲する（二重起動の防止。出典: IMP-2026-009 の一般化）。
+
+> 旧 stop/resume 機構（inflight-agents.json）と既知の制約の詳細は
+> [archive/orchestration-frozen-mechanisms.md](./archive/orchestration-frozen-mechanisms.md)
+> に凍結（2026-07 時点のハーネス挙動を前提とした機構。挙動改善時に復元判断）。

--- 同ファイル §起動方法（末尾）
 ## 起動方法
-Orchestrator はメインセッションとして起動するのが安定する。
+**Orchestrator 役はメインセッションが務める（正規ルート）。**
+
+- ローカル: `claude --agent orchestrator`（メインセッション起動）。
+- リモート: 通常セッションが本ポリシーに従い専門 Subagent を直接起動する。
+- Orchestrator 自体を `Agent` ツールの子エージェントとして起動する多段委譲は
+  **行わない**（孫通知の配送先・isolation 未継承の既知問題。凍結文書参照）。
```

```diff
--- .claude/agents/orchestrator.md（進め方 3・4 の抜粋）
-   - **L3 で** Sub-agent を **background で** 委譲する場合に限り（…）
-     `.claude/state/inflight-agents.json` へ追記するよう指示する（… 約 8 行）
-   - resume（会話再開）直後かつ `.claude/state/inflight-agents.json` に未処理エントリが（…）
-   - 上記は L3 background 委譲限定の仕組みだが、（… 約 10 行）
+   - resume・再開直後は、直前に委譲した Sub-agent の期待成果物の存在・更新時刻で
+     完了を冪等判定してから次を決める（正典: orchestration-policy.md §再開時の完了判定）。
```

（適用時は上記の要旨どおりに全文を整形する。行番号は 2026-07-20 時点）

## 反映先一覧（同期文書）

| ファイル | 役割（正典 / 同期） | 反映内容 |
| -------- | ------------------- | -------- |
| `docs/claude-code/orchestration-policy.md` | 正典 | 上記 diff（凍結節の移動 + 正規ルート明記 + 1 原則化） |
| `docs/claude-code/archive/orchestration-frozen-mechanisms.md` | 新規（凍結置き場） | 移動される節の全文 + 凍結理由・復元条件 |
| `.claude/agents/orchestrator.md` | 同期 | 進め方 3・4 の縮約 |
| `docs/claude-code/development-workflow.md` | 同期 | Orchestrator 起動記述があれば正規ルート文言に合わせる |
| `CLAUDE.md` §開発ワークフロー | 同期 | 「`claude --agent orchestrator` が指揮」の文言を「メインセッションが Orchestrator 役（ローカルは `claude --agent orchestrator`）」へ 1 行修正 |
| `docs/claude-code/usage-guide.md` | 同期 | 起動方法の記述があれば同上 |

## リスク

- 副作用: 将来ハーネス側の通知配送・isolation 継承が改善された場合に多段委譲を再評価する
  余地を失う → 凍結（archive へ移動）であり削除ではない。復元条件を凍結文書に明記する。
- 並列 background 委譲時の完了管理が「成果物冪等判定」1 原則に依存する → 直近 2 タスクで
  この原則単体で機能した実績あり。`inflight-agents.json` が必要になる規模の並列委譲は
  現運用（Codex 委譲中心）では発生していない。
- 指示過多の懸念: なし（正味約 76 行削減）。

## 評価方法

- 対象 eval ケース: `.claude/evals/cases/` の L2/L3 オーケストレーション系ケースで
  before/after 採点（IMP-2026-007/008 評価で使用したケースを再利用）。
- 観測軸: 要件理解 / 実装整合性 / 不要作業量 / トークン効率（悪化 1 軸でも不採用）。
- 実運用確認: 次の L2/L3 タスク 1 件を正規ルートのみで完走し、「成果物なし完了」再発ゼロ。

## 評価結果

（agent-evaluator の回帰評価は採否判断前に実施し、evaluations/IMP-2026-028.md へ記録）

## 採用または却下理由

（人間承認待ち）

## ロールバック方法

凍結文書から該当節を orchestration-policy.md / orchestrator.md へ戻す（適用コミットを
単一コミットにまとめ `git revert` 可能に保つ）。`inflight-agents.json` 運用も同時に復活する。
