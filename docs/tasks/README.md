# docs/tasks

実装指示・Codex ブリーフの置き場。

| パス                     | 用途                                   |
| ------------------------ | -------------------------------------- |
| [`codex/`](./codex/)     | 現行の Codex 委譲ブリーフ（正）        |
| [`archive/`](./archive/) | 完了済み Sprint 1 タスク指示書（履歴） |

新しいタスク指示は `codex/<feature>/` に置く。完了済みの旧形式 `sprint1-*` は
`archive/` を参照する。
