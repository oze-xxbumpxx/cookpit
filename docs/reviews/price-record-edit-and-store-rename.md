# レビュー記録: price-record-edit-and-store-rename

- 日付: 2026-08-06
- 対象: ブランチ `claude/l3-task-design-sprint7-10i25s` の `e6e639b`（バックエンド）/
  `3a30be2`（UI）。差分は `git diff 0e33f7e..3a30be2`
- 変更レベル: L3（Sprint 7 タスク2「価格記録の編集」+ タスク3「店舗のリネーム」）
- 判定: **Must 0 / Should 3 / Nice 5**。マージブロッカーなし
- 対応状況: **Should 3 件すべて対応済み**（2026-08-06・コミット後述）。Nice は N-3 のみ
  S-3 の対応に付随して解消、N-1/N-2/N-4 は申し送り

> **本レビューの実施者と限界**: reviewer Subagent を起動したが、13 時間超走った末に
> タスクレジストリから消え成果物を出さずに失われた（46 ツール呼び出しまでは進行を確認）。
> そのため **Orchestrator（メインセッション）が自ら実施した**。同一セッションが計画・実装の
> 委譲元でもあるため、独立性は Subagent レビューより低い。とくに「設計そのものの妥当性」への
> 批判的視点が弱い可能性がある点を限界として記録する。実行探索・機械的照合は実施済み。

## 実施した検証

| #   | 検証                                   | 方法                                                                 | 結果                                                                                                                                               |
| --- | -------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| V-1 | 試験 ID の計画 ⇔ 実装の全件照合        | 試験計画から ID を抽出し、実テストコードの ID と `comm` で差分を取る | 計画 102 件すべて実装済み（下記 注1）                                                                                                              |
| V-2 | 単位換算 4 方向（critical）            | 実テストコードの期待値を試験計画と突き合わせ                         | kg→g:200 / g→kg:10 / l→ml:20 / ml→l:10。試験計画どおり                                                                                             |
| V-3 | `Store.rename()` 可変化の波及          | `findAll()` の全呼び出し箇所を確認                                   | 全 UseCase が取得直後に DTO 化。長命な `Store` 参照・キャッシュ無し。安全                                                                          |
| V-4 | 404 判定の文字列依存（計画リスク R-1） | `NotFoundError` の実書式と UI の接頭辞を突き合わせ                   | `PriceRecord not found:` / `Store not found:` と一致。`Product not found:` は両方に不一致で汎用文言へ落ちる（設計どおり）                          |
| V-5 | ドメインの縮退・境界の実行探索         | `Product` / `Store` を直接実行（試験表に無い入力）                   | 下記 §実行探索の結果。欠陥なし                                                                                                                     |
| V-6 | 品質ゲート                             | `pnpm lint` / `type-check` / 各パッケージ `test`                     | lint 0 error（既存警告 1 件は未変更ファイル）/ type-check 5-5 / domain 408・application 275・api-contract 251・web 754・infrastructure 79 全 green |

> **注1**: 一次照合では `A-UPU-06/07/08`・`D-SRN-03` が未実装と出たが、**誤検出**だった。
> `it.each` のタイトルが `'A-UPU-05〜08: …'` / `'D-SRN-02/03: …'` という範囲表記で、
> 個別 ID の文字列検索に一致しなかったため。実体は 4 方向・両ケースとも実装・pass 済み。
> → この照合しにくさ自体を N-1 として記録する。

## 実行探索の結果（試験表に無い入力）

| 観点                                 | 入力                                                  | 結果                                                                                                                                         |
| ------------------------------------ | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 配列順序の保持（グラフの並びに影響） | 3 件中 2 番目を編集                                   | `r1,r2,r3` の順序が維持され、2 番目の店舗だけが変わる。**OK**                                                                                |
| `observedAt` のミリ秒精度            | `2026-01-01T09:08:07.123Z` の記録を編集               | ミリ秒まで完全一致で維持。**OK**                                                                                                             |
| 同一店舗 2 件 + `observedAt` 同値    | 別店舗の記録を同じ店舗へ付け替え（`observedAt` 衝突） | `cheapestStoreAt()` は例外を投げず候補を返す。設計書 §論点3 が「既存のタイブレーク差」として受容した挙動のとおり。**OK（クラッシュしない）** |
| 存在しない ID での更新               | `updatePriceRecord('nope', …)`                        | throw し、**件数・`updatedAt` ともに変化なし**（部分変更が残らない）。**OK**                                                                 |
| 正規化で空白になる店舗名             | `'　'`（全角空白）/ `' '`                             | どちらも `Store name is required` で拒否。**OK**                                                                                             |
| 前後空白つきの店舗名                 | `' ライフ '`                                          | 受理され**原文のまま保存**される（正規化後は `'ライフ'`）。→ N-2                                                                             |

## Should（マージ前の対応を推奨。ブロッカーではない）

### S-1: `PriceRecordEditDialog` の店舗一覧取得が失敗しても縮退しない — ✅ 対応済み

- 根拠: `apps/web/src/app/products/[id]/_components/price-record-edit-dialog.tsx:88-97`
- `initialize()` が `try` / `finally` のみで **`catch` を持たない**。結果として 2 つの穴がある。
  1. `client.api.stores.$get()` が例外を投げると `void initialize()`（99 行目）のまま
     **未処理の Promise 拒否**になり、ユーザーには何も表示されない。
  2. `!response.ok` の場合も黙って無視され、`stores` が空配列のまま残る。
- なぜ問題か: `storeId` は 82 行目で `record.storeId`（UUID）に**先に**設定されるため、
  `storeOptions`（66-69 行目）が placeholder 1 件だけになった状態で `storeId` に UUID が入る。
  select は選択肢に無い値を表示できず「店舗を選択」と出るのに、内部状態は元の店舗を保持する。
  ユーザーには「店舗が未選択に見える」「店舗を変更できない」が、理由は一切表示されない。
  価格・内容量の編集と保存自体は成功する（`canSubmit` は `storeId !== ''` で true）ため、
  **データは壊れないが説明のない機能低下になる**。
- **同じタスク内の姉妹コンポーネントは両方とも対応済み**なのが問題の本質:
  - `store-rename-dialog.tsx:72` は usage 取得を `catch` し、試験計画に **SRD-05**
    （「usage 取得失敗時も操作を継続できる」）として観点が入っている。
  - 既存 `price-record-form.tsx:65,131` は `storesErrorMessage` state を持ち `catch` している。
  - **編集ダイアログだけ同じ観点が抜けている**（試験計画 §8-1 の PRED-01〜10 に
    店舗一覧取得失敗のケースが無い）。試験計画に無い観点は実装にもテストにも現れない、
    という先行タスク（shopping-list-price-comparison）と同型の穴。
- 修正案: `catch` を追加し、`storesErrorMessage` state（`PriceRecordForm` と同じ命名）で
  「店舗の取得に失敗しました。」を表示する。`!response.ok` も同じ経路へ寄せる。
  併せて試験計画 §8-1 に `PRED-11`（取得失敗時にエラー文言が出て、価格・内容量の編集と保存は
  継続できる）を追加する。

### S-2: 試験計画 §8-1 に店舗一覧取得失敗の観点が無い（S-1 の上流原因） — ✅ 対応済み

- 根拠: `docs/tests/price-record-edit-and-store-rename.md` §8-1（PRED-01〜10）
- `StoreRenameDialog` 側には SRD-05 として縮退観点があるのに、`PriceRecordEditDialog` 側は
  「補助情報（店舗一覧）の取得失敗」を扱っていない。S-1 を直すだけでなく、
  **試験計画にも観点を戻さないと同じ抜けが再発する**（試験計画が正典なので）。
- 修正案: S-1 の修正案に含めた `PRED-11` を追加する。

### S-3: `it.each` の範囲表記タイトルが試験 ID の追跡を壊す — ✅ 対応済み

- 根拠: `packages/application/tests/product/product-use-cases.test.ts:490`（`'A-UPU-05〜08: …'`）、
  `packages/domain/tests/shared/store.test.ts:85`（`'D-SRN-02/03: …'`）
- なぜ問題か: このプロジェクトは試験 ID で要件 ⇔ 試験 ⇔ 実装を追跡している
  （試験計画 §1 の対応表）。範囲表記にすると**個別 ID での検索が一致せず、
  未実装と誤判定される**。本レビューの一次照合が実際に 4 件を誤検出した（注1）。
  「テストがある」ことの確認コストが上がり、監査で偽の欠落を報告する原因になる。
- 修正案: `it.each` のタイトルにパラメータとして ID を含める
  （例: `'A-UPU-%s: 単位換算 %s 方向で…'` として `['05','kg→g']` を渡す）。
  もしくは fixture 配列の各要素に ID を持たせてタイトルへ展開する。

## Nice（申し送り。今スプリントで必須ではない）

- **N-1**: 上記 S-3 と同根の運用課題として、「試験計画の ID がすべて実装されているか」を
  機械的に照合するスクリプトが無い。本レビューで使った抽出・`comm` 照合は再利用価値がある。
  `quality-gates` か `validate-deliverables` へ組み込む候補。
- **N-2**: 店舗名は前後空白つき（`' ライフ '`）でも原文のまま保存される。
  同名判定は正規化後なので重複は防げるが、表示に空白が残る。
  **`Store.create()` から続く既存挙動**であり本タスクが持ち込んだものではない。
  保存時に `normalizeStoreName()` の結果を入れるかは別タスクの判断。
- **N-3**: `A-UPU-05〜08` の fixture が持つ `from`（編集前の単位）が**テスト本体で未使用**。
  `seededPriceRecord()` は `packageSize` を `Quantity.of(3, '個')` 固定で作るため、
  試験計画が書いた「編集前は 1kg」という前提は実際には再現されていない。
  単価は編集後の (price, packageSize) だけから再計算されるので**結論は変わらない**が、
  fixture の `from` は誤解を招く死データ。削除するか、実際に seed へ反映する。
- **N-4**: `price-record-edit-dialog.tsx:104` に `eslint-disable-next-line
react-hooks/exhaustive-deps` がある（依存は `[record?.id]`）。同じ記録を開いている間に
  親の `product` が更新されると、`record` の中身が変わっても再初期化されない。
  成功時はダイアログを閉じるため実害は出にくいが、抑制の理由はコメントどおり
  「同じ記録の再描画では走らせない」であることを申し送る。

## 確認できた良い点

- **設計の確定事項が実装で守られている**: `id`/`observedAt` の維持を Domain 側で保証
  （`product.ts` の `updatePriceRecord` が `original.observedAt` を渡す形）、
  `PriceRecord` の不変性維持（差し替え方式）、単価の再計算を Application 層が担う責務分担、
  同名チェックの自己衝突除外（`duplicate.id.equals(storeId)`）。
- **Infrastructure・DB スキーマが本当に変更不要だった**。設計書が根拠として挙げた
  `onConflictDoUpdate` の既存 upsert がそのまま機能し、差分は 0 行。
- **ADR-0015 の切り分けが実装にも現れている**: `DuplicateStoreNameError` の JSDoc が
  「作成・リネーム」へ更新され、メッセージも操作中立な文言へ直っている
  （リネーム経路から `Cannot create Store` が返る食い違いの解消）。
- **試験 ID の重複を実装中に是正した**: `WH-S-06` が先行タスクの DELETE テストと衝突していた件を
  `WH-S-07〜13` へ繰り下げ、試験計画・実装計画の参照も追随させ、注記も残している。
- TDD の Red が実際に確認されている（Step 1: 12 件失敗 `is not a function` → 402/402、
  Step 3/4: module-not-found → green、Step 11: 4/12 失敗 → 12/12）。

## 実画面確認（2026-08-06 実施・MB-01〜05 全 PASS）

PGlite dev + Playwright / 390px。詳細と是正内容は試験計画 §13 の実施結果ブロックを参照。

| #     | 結果 | 要点                                                                                                            |
| ----- | ---- | --------------------------------------------------------------------------------------------------------------- |
| MB-01 | PASS | 編集ダイアログが初期値（スーパーA / 298 / 3個）を表示し、390px で横スクロール・文字欠け・ビューポート外要素なし |
| MB-02 | PASS | 6 件中 直近 5 件のみに編集アイコン（5 個）。最古の 201 円の行が一覧に存在しない                                 |
| MB-03 | PASS | 1kg → 500g 編集で単価表示が「1,000円 / 1kg」→「200円 / 100g」に変わり、旧値は残らない                           |
| MB-04 | PASS | 価格記録 2 件の店舗で件数入り警告が出て、0 件の店舗では警告文が一切出ない                                       |
| MB-05 | PASS | リネーム後、最安店舗カードと最近の記録の店舗名が両方新名になり、旧名が画面から消える                            |

ブラウザコンソールのエラー: なし。

### 実画面確認で判明した試験計画の誤り（是正済み）

- **MB-03 の前提記述が誤っていた**。「編集前の単価表示は 100円 / 100g」としていたが、
  `kg` 記録の表示基準は `1kg`（`product-format.ts` の `WEIGHT_BASIS` が `kg` に
  `factor: 10` / `label: '1kg'`）で、実際は「1,000円 / 1kg」。**先行タスクと同型の
  「PASS 基準が実物と違う」問題**で、当初この前提を検証しない判定にしていたため
  一度は誤って PASS が出ていた。前提の検証を PASS 条件に含めた上で是正した。
- **手順の落とし穴**: PGlite の dev DB を再シードするときは dev サーバーを止めてから行う。
  起動中に `.pglite-dev` を作り直すと、サーバーが削除済みディレクトリのハンドルを掴んだまま
  古い状態を返し続け、**前提を満たしていないのに PASS に見える**（実際に一度誤判定した）。

### 新たな申し送り（N-5）

- **N-5**: 編集で内容量の単位を変えると単価表示の分母（基準）も変わる
  （「1,000円 / 1kg」→「200円 / 100g」）。DB の正準値は一貫しており店舗間比較は壊れないが、
  同じ記録の編集前後を目で比較しにくい。編集機能が入って初めて「同じ記録の単位を変える」
  経路が生まれたため記録する（試験計画 §11 にも申し送り済み）。

## 未実施

- security-reviewer による専門レビュー。MVP1 は認証なし（ADR-0003）で新たな攻撃面も
  無い想定だが、L3 なので実施が望ましい。

## Should の対応記録（2026-08-06）

### S-1 の対応

`price-record-edit-dialog.tsx` に `storesErrorMessage` state と `catch` を追加し、
例外・`!response.ok` の両方で「店舗の取得に失敗しました。」を表示するようにした。
さらに**編集対象の記録が指す店舗を選択肢のフォールバックとして出す**ようにした
（`missingCurrentStore`）。これがないと `storeId` に値があるのに選択肢が無く、
select が「店舗を選択」と表示して未選択に見える状態が残るため。

試験は `PRED-11`（2 パターンの `it.each`）・`PRED-12` を追加。Red で 3 件の失敗を確認して
から実装した。

### S-2 の対応

試験計画 §8-1 に `PRED-11` / `PRED-12` を追加し、§1 対応表の E-08 行・§2 の網羅表・
§14-1 の完了条件の `PRED-01〜10` を `PRED-01〜12` へ更新した。

### S-3 の対応

`it.each` の fixture に**試験 ID を literal で持たせ**、タイトルを `'%s: …'` に変えた
（`['A-UPU-05', 'kg→g', …]` / `['D-SRN-02', '']`）。

当初は `'A-UPU-%s'` + `['05', …]` の形にしたが、**これでは不十分だった** —
`%s` の展開は実行時に起きるので、ソースには `A-UPU-05` という文字列が現れず
grep で追跡できないままだった。ID 全体を fixture 側に置くことで、実行時の表示名と
ソース検索の両方で一致するようになった。

対応後、試験計画の 102 件すべてがソース検索で実装済みと確認できる
（`comm -23` の差分が空）。

### N-3 の解消（S-3 の対応に付随）

`A-UPU-05〜08` の fixture から未使用の `from`（編集前の単位）を削除した。
併せて、seed される記録の `packageSize` が `3個` 固定で編集後の単位（kg/g/l/ml）とは
**意図的に別次元**であり、単価の再計算が「保存済みの単位」ではなく「渡された単位」を
使うことを担保している、という意図をコメントで明示した。

### 対応後の品質ゲート

lint 0 error（既存警告 1 件は未変更ファイル）/ type-check 5-5（キャッシュ無効で実行）/
domain 408・application 275・api-contract 251・web **757**（754 → +3）・infrastructure 79。
