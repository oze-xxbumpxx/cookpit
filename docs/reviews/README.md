# docs/reviews

feature 単位のレビュー結果（`<feature-name>.md`）を置く。

- Orchestrator 経路: reviewer が作成（主に Level 3。L2 は必要なら）。
- Codex 委譲経路: `review-codex-implementation` の受け入れレビュー結果を**必須で追記**
  （正本。PR 本文だけの記録は不可。複数 Task なら Task 単位で追記）。
