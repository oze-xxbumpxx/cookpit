node_modules
.next
dist
.turbo
pnpm-lock.yaml
.obsidian
# Next.js がビルド時に生成する型参照ファイル（git 管理外）。
# CI は format → build の順のため CI では存在しないが、ローカルで build 済みだと
# format:check が偽の失敗になるため除外する。
apps/web/next-env.d.ts
# 日次ログは自動追記が多く、整形漏れで CI を落とさないため除外
logs
# 構成ファイル（CLAUDE.md / .claude 配下）は改善サイクルの承認境界の対象のため
# 一括整形・CI format チェックの対象外とする
.claude
CLAUDE.md
