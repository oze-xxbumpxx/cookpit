# 2026-07-31

## 今日のタスク

- Sprint：Sprint 2
- 作業ブランチ：`claude/reuse-config-other-projects-n8wqmj`
- 変更レベル：**L1 相当**（調査・方針提案のみ。コード・設定の変更なし）

ユーザーからの相談 —「今の Claude Code 構成を他のプロジェクトにも活かしたい。
どのようにすれば良い？」。構成の移植性を実測し、配布方式と段階手順を提案する。

## やったこと

- [x] `.claude/` と `docs/claude-code/` を全ファイル棚卸し（行数計測 + 固有名詞 grep）
- [x] 移植性を 3 層に分類し、層ごとの行数と移植難度を提示
  - 実行系（hooks 874 + lib 525 + scripts 2,228 = 3,627 行）→ ほぼそのまま動く
  - 指示系（agents 762 + skills 1,312 + rules 131 = 2,205 行）→ 骨格は汎用・本文にスタック前提
  - 方針（`docs/claude-code/*.md` 直下 1,867 行）→ 同上
  - 実績・履歴（evals 3,391 + improvements 11,055 + archive 3,382 = 17,828 行）→ 固有資産
- [x] 実行系のプロジェクト固有箇所を行単位で特定（下記「気づき」参照）
- [x] 配布方式 4 案（Plugin / テンプレート / git subtree / `~/.claude/`）を比較し **Plugin 化を推奨**
- [x] 段階手順 Phase 1〜3 と移植ティア（Minimal / Standard / Full）を提案
- [x] ユーザーへ確認事項を提示（配布方式の可否・移植先プロジェクトの規模）→ **未回答・着手前**

成果物はチャット回答のみ。設計書は未作成（着手承認前のため）。

## 気づき・メモ

- **実行系はほぼ汎用だった。** 3,627 行のうち移植を阻む箇所は実測で数点のみ。
  - `.claude/lib/harness-paths.mjs:20` — `STATE_NAMESPACE = 'cookpit-harness'`
  - `COOKPIT_TZ` 環境変数名（`check-improvement-cycle.mjs:102` /
    `session-briefing.sh:13` / `sprint-summary.sh:13`）
  - `pnpm` 決め打ち（`run-quality-gates.sh` / `detect-project-commands.sh`）。ただし
    両者は `has_script` で存在検出する設計なので、パッケージマネージャ検出を足すだけで済む
  - `settings.json` の `hooks` は `$CLAUDE_PROJECT_DIR` 相対で**完全に汎用**。
    固有なのは `permissions`（pnpm / terraform / sst 前提）だけ
- **スタック依存が濃いのは 2 ファイルに集中している。**
  `.claude/scripts/check-codex-implementation.mjs`（固有名詞 10 箇所・Tailwind / Next 前提）と
  `record-task-metrics.sh`（14 箇所）。逆に言えば**この 2 本を切り離せば実行系は移植可能**。
- **`.claude/rules/` は plugin で配布できない。** あの自動注入は「プロジェクトローカルに置いた
  `.md` が project instructions として読まれる」挙動（`.claude/rules/README.md` の 2026-07-28
  実測記録）で、plugin の配布プリミティブには rules ディレクトリの概念が無い。
  汎用の `coding-standards.md` は **plugin skill か CLAUDE.md テンプレート**として配る必要がある。
  `domain-layer.md` / `presentation-layer.md` は Clean Architecture + Next.js 以外に
  持ち込むと**有害**（常時トークンを消費する上に前提が合わない）。
- **事象出典は移植先で dead link になるが、消してはいけない。** `recipe-servings 事象2` 等は
  指示の根拠そのもの。参照形式を「出典: <プロジェクト>/<事象>」へ統一し、移植先では
  「他プロジェクトの実績」として読める形にするのが妥当（Phase 2 の論点）。
- **過剰移植のリスクを明示した。** この構成は設定 7,700 行 + 履歴 17,800 行で、Cookpit の
  規模に合わせて育ったもの。L0〜L3 分類・evals 回帰評価・improvement cycle を全部持ち込むと
  小規模プロジェクトでは工程が完全に過剰になる。**移植先ごとにティアを選ぶ**前提にした。
- Cookpit 自身も plugin 利用側へ転換すると dogfooding になり、改善サイクルの成果が
  全プロジェクトへ伝播する（Phase 3 の狙い）。

## AI ツール活用記録

| ツール      | 依頼内容                                     | 結果・気づき                                                                     |
| ----------- | -------------------------------------------- | -------------------------------------------------------------------------------- |
| Claude Code | 構成の移植性調査と他プロジェクトへの展開方針 | 固有名詞 grep + 行数計測で「実行系はほぼ汎用・阻害要因は数行」と定量的に確定できた |
| Codex       | —                                            | 今回は委譲なし                                                                   |
| Gemini      | —                                            | —                                                                                |
| Perplexity  | —                                            | —                                                                                |

## 次回やること

- [ ] **ユーザーの回答待ち**（本件の前提）。①配布方式は Plugin 化で良いか
      ②最初の移植先プロジェクトの規模（ティア決定に必要）
- [ ] 承認後 **Phase 1: 抽象化点を潰す**（L2・構成ファイル変更なので承認境界は PR レビュー）
      1. `STATE_NAMESPACE` を `package.json` の `name` 由来か `HARNESS_NAMESPACE` 環境変数へ
      2. `COOKPIT_TZ` → `HARNESS_TZ`（旧名も読む後方互換つき）
      3. `detect-project-commands.sh` に npm / yarn / bun 検出を追加
      - 既存挙動を変えないため Cookpit 側の回帰リスクはほぼゼロ。`.claude/tests/` の
        `harness-paths.test.mjs` が namespace を参照しているか要確認
- [ ] Phase 2: core / stack 境界を引く（agents からスタック依存記述を rules 側へ寄せる）
- [ ] Phase 3: `.claude-plugin/plugin.json` + `marketplace.json` で別リポジトリへ切り出し
- [ ] 前回（7/27 セッション2）からの継続 — 未着手のまま持ち越し
      - [ ] 本番の店舗を 3 件へ減らす（ユーザー操作）
      - [ ] Vercel Function を hnd1 へ移すか判断（Neon 側リージョン確認が前提）
      - [ ] 商品詳細の DB 往復削減（`findById` 2 回）と recharts の遅延読み込み
      - [ ] フォントをさらに削るか（見た目の判断が必要）
      - [ ] PR #118 のレビュー・CI、products フォームへの `useLeaveConfirmation` 適用、
            作り方の手順（steps）の並べ替え

## 所要時間

約 30 分（棚卸し・計測 20 分 / 方針整理・回答 10 分）

---

## セッション2 — Phase 1: 名前空間と環境変数の脱プロジェクト名（L2）

### きっかけ

ユーザーが「配布方式でいきたい」と回答（= Plugin 化）。加えて抽象化範囲の選択で
**パッケージマネージャは抽象化しない**（全プロジェクト pnpm 前提）、移植ティアは未定と決定。
Phase 1 は配布方式に依存しないため、先に着手した。

### やったこと

- [x] 変更レベル判定 **L2**（複数ファイルの既存処理変更・後方互換を壊さない・
      新規 API / DB / アーキテクチャ変更なし）。feature-name `harness-portability`
- [x] L2 成果物 3 本を作成（設計書 / 実装計画 / 試験計画）
- [x] `resolveNamespace()` を実装。`HARNESS_NAMESPACE` → `package.json` の `name` +
      `-harness` → `claude-harness`。パスへ連結するため単一セグメントへ無害化
- [x] `HARNESS_TZ` / `HARNESS_GAP_CAP_MIN` を追加し旧 `COOKPIT_*` を後方互換で維持（5 ファイル）
- [x] 名前空間導出の試験 8 件を追加（48 → 56 件・全 PASS）
- [x] 品質ゲート: harness / lint / type-check / build / test すべて PASS
- [x] 振り返り候補・backlog 追記・メトリクス（TASK-2026-007）
- [x] コミット `d1bf604`（実装）+ 追加コミット（`harness-state.md` の add 漏れ・メタ成果物）

### 気づき・メモ

- **移行が不要になる設計を選べた。** `package.json` の `name` が `cookpit` なので
  `${name}-harness` = `cookpit-harness` で**現行の状態ディレクトリと完全一致**する。
  サフィックスを `-harness` にしたのは偶然ではなく、現行値との一致を設計目標に置いたから。
  実装計画で「導出結果が一致しなければ中断してユーザーへ報告」を独立ステップ（手順 3）に
  したうえで実測 → 一致を確認。**移行スクリプトを書いてから不要と気づくより安い。**
  将来この前提が壊れたら検出できるよう、回帰テストで `cookpit-harness` を固定した。
- **`STATE_NAMESPACE` の参照元が 2 ファイルしかないと先に確認したのが効いた。**
  `harness-paths.mjs` 自身とテストだけで、他 11 ファイルは `statePath` / `safeStatePath` /
  `resolveReadablePath` 経由で名前空間を知らない。**前セッションで挙げた
  「テストが namespace リテラルをアサートしているかも」という懸念は外れていた**
  （シンボルで import していた）。懸念は調べれば消える。
- **固有名詞の棚卸しを個別の変数名で grep して取りこぼした。** `COOKPIT_TZ` を列挙して
  探したため `COOKPIT_GAP_CAP_MIN` を見落とし、実装中に発見して設計書 R-3 を更新した。
  目的はプレフィックスの棚卸しなのに検索式が葉を指していた。
  **棚卸しは `grep -rn 'COOKPIT_'` のようにプレフィックス単位でやり、全ヒットを分類する。**
  分類の結果 `COOKPIT_METRICS_*`（11 箇所）は「利用者が設定しない内部受け渡し」として
  対象外に仕分けた。この仕分け自体は有益だった。
- **Prettier が Markdown を静かに壊した。これが今回いちばん危ない発見。**
  `--write` で `GAP_CAP_MIN` が `GAP*CAP_MIN` に、`'COOKPIT_'` が `'COOKPIT*'` になった
  （`_` を emphasis と解釈）。さらに表セルに書いた `HARNESS_TZ || COOKPIT_TZ` の `|` が
  列区切りと解釈され、区切り行に列が 4 つ増えた不正な表になった。
  **内容が変質しているのに品質ゲートは PASS する。** Prettier は Markdown として正しく
  整形しており、素のテキストでコード断片を書いた側の問題。
  → 識別子・シェル式は必ずバックティックで囲む。`--write` の後は生成物を目視する。
- **`git add` のパス列挙で 1 ファイル漏らした。** 新規 3 本を明示 add する必要があったので
  パスを列挙したが、実装計画の「変更対象ファイル」表に載っていた
  `docs/claude-code/harness-state.md` が抜けた。コミット直前の `git status` にも出ていたのに
  照合しなかった。Stop hook の未コミット検出で気づいた。
  **実装計画に変更対象ファイル表があるのだから、コミット前にそれと `git status` を突き合わせる。**
- **`harness-paths.mjs` の公開 API 14 件中 7 件が元から未テスト**と判明（試験計画の
  メソッド網羅チェックで発見）。`stateDir` / `statePath` / `safeStatePath` /
  `resolveReadablePath` / `hasSafePermissions` / `repoRoot` / `legacyState*`。
  本タスク起因ではなくスコープ外としたが、**Phase 2 で他プロジェクトへ配る前に埋めたい**
  （配布後は不具合の影響範囲が広がる）。backlog の申し送りへ起票した。
- リモート環境は `node_modules` 未インストールで、初回のゲートは lint / type-check / build /
  test が環境要因で FAIL した（harness だけ PASS = Node 標準のみで動く証拠）。
  `pnpm install --frozen-lockfile` 後に再実行して実測し直した。**環境要因の FAIL を
  変更起因と混同しないこと。**
- `format-check` の 3 件 FAIL は `git stash` で HEAD 時点でも同一と確認した既存 FAIL。
  いずれも `accepted/` と `archive/` 配下で「歴史的ドキュメントは書き換えない」方針の対象。

### 品質ゲート

`pnpm test:harness`（56 件）/ `pnpm lint`（0 errors・既存 warning 1）/ `pnpm type-check` /
`pnpm build` / `pnpm test` すべて PASS。`format-check` は既存 FAIL 3 件のみ（対象外）。

### 次回やること（本件から）

- [ ] **Phase 2 と Phase 3 の順序をユーザーと決める。** Phase 2（指示系の core / stack 分離）は
      移植ティアが決まらないと価値が測れない（Minimal 相当なら agents 分離の意味が薄い）。
      移植先未定なら **Phase 3（Plugin リポジトリの骨格）を先**にする選択もある
- [ ] Phase 2 の対象として残っている実行系 2 本:
      `check-codex-implementation.mjs`（Tailwind / Next 前提 10 箇所）と
      `record-task-metrics.sh`（`COOKPIT_METRICS_*` 11 箇所）
- [ ] `.claude/rules/` は Plugin の配布プリミティブに含まれないため、汎用ルールを
      skill / CLAUDE.md テンプレートのどちらで配るかを Phase 2 で決める
- [x] `harness-paths.mjs` の未テスト公開 API 7 件への試験追加（配布前が望ましい）
      → **セッション3 で完了**

---

## セッション3 — Plugin の切り方決定と harness-core の土台固め（L1）

### きっかけ

ユーザーから「移植ティアを決めますか？」。移植先が未定のままティアを 1 つ選ぶのは
無理があるが、**Plugin 配布なら「選択」ではなく「切り方」を決めればよい**と整理した
（install 時にプロジェクトごとに選べる）。切り方は Phase 3 の前提でもある。

### やったこと

- [x] 依存グラフを実測（hooks / scripts の import・Skill からのスクリプト参照）
- [x] 依存が閉じた 3 段構成を提案 → **ユーザーが 3 分割を選択**
  - `harness-core` 1,919 行（依存なし）
  - `harness-workflow` +2,324 行（core 依存）
  - `harness-improvement` +2,149 行（workflow 依存）
- [x] **未テスト公開 API を切り出し前に埋める**とユーザーが選択 → 即日実施（L1）
- [x] `.claude/tests/harness-paths.test.mjs` に 11 件追加（56 → 67 件・全 PASS）
- [x] 公開 API 13 件すべてに試験があることを機械照合で確認
- [x] backlog の該当項目を「完了（2026-07-31）」へ更新
- [x] 品質ゲート: harness / lint / type-check / test すべて PASS

### 気づき・メモ

- **「ティアを選ぶ」という枠組み自体が Plugin 配布では不要だった。** 前セッションまで
  「移植先が決まらないとティアが決まらない」と考えて Phase 2 を止めていたが、
  マーケットプレイスに複数 plugin を並べれば選択は install 時に後ろへ倒せる。
  **決められないのは「選択」で、決めるべきは「切り方」だった。** 依存関係を実測したら
  切り方は移植先に依存せず決まった。ブロッカーの正体を問い直すと消えることがある。
- **`close-session` が Standard 層に置けないという依存の矛盾を発見した。**
  skill 自身が「kickoff-session と対になる」と書いているのに、依存先 5 本のうち 4 本
  （`record-task-metrics.sh` / `collect-task-metrics.mjs` / `estimate-session-time.mjs` /
  `check-review-coverage.mjs`）はメトリクス＝improvement 層。そのまま切ると
  `harness-workflow` だけ入れた人が「開始できるが終了できない」状態になる。
  → Phase 2 で `close-session` を「ゲート + ログ + コミット」と「メトリクス + 振り返り」に
  分割する作業が追加で必要。**依存グラフを引かずに層を切ると壊れた組み合わせを配ることになる。**
- **前セッションの見積りを 1 件訂正した。** `check-review-coverage.mjs`（29 行）を Standard に
  数えていたが、呼び出し元は `check-improvement-cycle.mjs` と `close-session` だけで
  両方 improvement 層。ファイルの「役割の見た目」ではなく**呼び出し元**で層を決めるべきだった。
- **テストが実領域を汚さないことを明示的に確認した。** sandbox の root に `package.json` が
  無いため導出が `claude-harness` になり、もし `env` / `home` を渡し忘れていたら
  `~/.local/state/claude-harness/` が実際に作られる。`ls -d` で「作られていない」ことを
  確認し、既存の `cookpit-harness/` が無傷であることも併せて見た。
  **副作用のある関数のテストは「汚していない」ことまで確認して初めて完了。**
- 前回の教訓（Prettier が `_` を emphasis に変える）を backlog 追記でも適用し、
  `--write` 後に `[A-Za-z]+\*[A-Za-z]+` で誤変換チェックを回した。誤変換なし。

### 品質ゲート

`pnpm test:harness`（67 件）/ `pnpm lint`（0 errors・既存 warning 1）/ `pnpm type-check` /
`pnpm test` すべて PASS。

### 次回やること（本件から）

- [ ] **Phase 2**（指示系の core / stack 分離）。3 分割が決まったので着手可能。追加論点:
      - `close-session` の 2 分割（workflow 層 / improvement 層）
      - `check-codex-implementation.mjs`（Tailwind / Next 前提 10 箇所）と
        `record-task-metrics.sh`（`COOKPIT_METRICS_*` 11 箇所）の脱スタック依存
      - `.claude/rules/` は Plugin で配布できないため、汎用ルールを skill /
        CLAUDE.md テンプレートのどちらで配るか決める
      - 事象出典の参照形式を「出典: &lt;プロジェクト&gt;/&lt;事象&gt;」へ統一
- [ ] **Phase 3** `.claude-plugin/plugin.json` + `marketplace.json` を別リポジトリへ。
      Cookpit 自身も利用側へ転換して dogfooding する

---

## セッション4 — Phase 2: Plugin 3 層の境界確定と層をまたぐ依存の解消（L3）

### きっかけ

ユーザーが「お願いします」= Phase 2 着手。本番データ側は店舗削除がユーザー操作、
Vercel リージョン判断が `.env` 待ちで動かせないため Phase 2 を選択した。

### やったこと

- [x] 変更レベル **L3**（大規模リファクタ）と判定。feature-name `harness-plugin-split`
- [x] 要件定義を作成（F-1〜F-7 / N-1〜N-3 / 正常系 N-01〜04 / 異常系 E-01〜03 / 境界 B-01〜04）
- [x] 未決 3 点をユーザーへ確認 → **D-1 Cookpit ローカルに残す / D-2 CLAUDE.md 断片 /
      D-3 プロジェクト名前置**が確定
- [x] 設計書・ADR-0014・実装計画・試験計画を作成
- [x] 層マニフェスト（`docs/claude-code/plugin-layer-manifest.md`）で全 72 ファイルを
      core 17 / workflow 22 / improvement 18 / cookpit-local 14 / 層外 1 へ割り当て
- [x] `close-session` を分割 → `record-metrics-and-reflect`（新規・improvement 層）
- [x] `classify-change` の無条件依存を修正（層照合で新規発見）
- [x] `record-activity` / `estimate-session-time` を improvement → core へ再割り当て
- [x] `COOKPIT_METRICS_*` → `HARNESS_METRICS_*`（13 箇所）
- [x] 出典を `cookpit/<feature>` 形式へ（パス形式 32 + 素の feature 名 7 = 39 箇所）
- [x] CLAUDE.md 断片（`.claude/templates/claude-md-coding-standards.md`）を作成
- [x] レビュー記録・振り返り候補・backlog 起票・メトリクス（TASK-2026-008）
- [x] 品質ゲート: harness(67) / lint / type-check / build / test すべて PASS
- [x] コミット `049dfd3` + メタ成果物

### 気づき・メモ

- **D-1 の決定で作業量が激減した。** 当初「`check-codex-implementation.mjs` の
  固有名詞 10 箇所を脱スタック依存」と見積もっていたが、実体は 861 行の Tailwind クラス
  辞書検証器で**中身が丸ごとスタック固有**だった。汎用化する対象ではなく「どこに置くか」の
  配置問題。**見積りの粒度が「grep のヒット数」だと中身の性質を見落とす。**
  ユーザーが「ローカルに残す」を選んだ結果 1,418 行に一切触れずに済んだ。
- **層をまたぐ依存の機械照合が未知の欠陥を 1 件発見した。** 認識していたのは
  `close-session` の 1 件だけだったが、`classify-change` の L2 Agent 早見表が
  `reflection-agent`（improvement）と `contract-designer`（cookpit-local）を**無条件指定**
  していた。表の 1 セルは人手レビューで見落としやすい。**境界を作る変更では、境界違反の
  検出を grep レベルの受け入れ条件に落とすと、想定外の違反が拾える。**
- **層は「名前」ではなく「依存の向きと利用者」で決まる。** 割り当てを 2 回訂正した。
  `check-review-coverage.mjs` は名前から workflow 相当に見えたが呼び出し元は improvement の
  2 本だけ。`record-activity` / `estimate-session-time` は「メトリクス系」に見えたが、
  依存は harness-paths のみで `write-work-log`（core）の「所要時間」欄を埋めるため core が
  正しい。**分類の前に依存グラフを機械的に取る。**
- **棚卸しの母集団を「形式」で数えて取りこぼした（Phase 1 と同じ類型・累計 2 回）。**
  出典を「38 箇所」と数えたが、その grep はパス形式だけを拾うもので、
  `出典: shopping-list-core 事象 2` のような素の feature 名 11 箇所が母集団から落ちていた。
  要件 F-7 を部分充足のまま「完了」と報告しかけ、自己レビューで検出した。
  Phase 1 の `COOKPIT_GAP_CAP_MIN` 見落としと根が同じ。→ **累計 2 回で昇格条件を満たすため
  backlog へ起票**（「網羅した」と主張するなら補集合側の検索を 1 本添える）。
- **L3 成果物が揃っていると誤って報告した。** document-policy の L3 は
  `docs/reviews/<feature>.md` も必須なのに、報告では要件・設計・実装計画・試験計画・ADR・
  層マニフェストを挙げて「揃っている」と書いた。層マニフェストは policy の成果物ではなく、
  reviews が欠けていた。Stop hook の改善サイクル・チェックで気づいた。
  **成果物の充足は policy の一覧と 1:1 で照合する。手持ちの成果物を並べて数えても
  「欠けているもの」は見つからない**（これも事象 1 と同根＝補集合を見ていない）。
- **検算の数値を一覧表と別に手書きして食い違わせた。** マニフェストで core を見出し「16」、
  improvement を見出し「12」・検算表「16」と書き、`templates/` の新規分も未計上。
  一覧の行が 1 行 1 ファイルでない箇所があり行数 ≠ 件数だった。試験計画の観点 11 で検出し、
  ディレクトリ別の実数から積み上げる形へ改めた。
- **自己レビューになった点は記録に明記した。** 本セッションは Subagent 委譲を行わない制約
  だったため reviewer Agent に委譲できず、Orchestrator 役が自己レビューした。Must 1 件を
  検出できたが、設計自体の妥当性は原理的に検出しにくい。第三者確認は PR レビューへ委ねる旨を
  レビュー記録に書いた。

### 品質ゲート

`pnpm test:harness`（67 件）/ `pnpm lint`（0 errors・既存 warning 1）/ `pnpm type-check` /
`pnpm build` / `pnpm test` すべて PASS。`format-check` は既存 FAIL 3 件のみ
（`git stash` で HEAD 時点と同一と確認。`accepted/` と `archive/` 配下で対象外方針）。

### 次回やること（本件から）

- [ ] **Phase 3**: `.claude-plugin/plugin.json` + `marketplace.json` を別リポジトリへ。
      層マニフェストが入力。Cookpit を利用側へ転換して dogfooding。
      Phase 3 で解く宿題（レビュー記録の Should から）:
      - S-1: workflow 層の Skill が Cookpit 固有ドキュメント（`docs/06-ai-tools.md` /
        `logs/2026-06-24.md` / `archive/...`）を参照している。切り出し時に棚卸しが必要
      - S-2: 「core だけで動く」が未実証。要件 B-01〜B-03（`logs/` や `docs/designs/` が
        無い場合）を実 install で検証する
      - S-3: `settings.json` は層外だが、hooks 配線が無いと core の Hook 3 本が動かない。
        Plugin README に推奨設定例を載せる
      - `plugin.json` の依存宣言がどこまで強制されるかの調査（要件 E-01）
      - `evals/cases/` 10 件の汎用 / スタック固有の線引き
- [ ] backlog の新規起票分（棚卸しの母集団定義・累計 2 回）を proposal 化するか判断
- [ ] 前回からの継続（未着手のまま持ち越し）— 本番の店舗を 3 件へ減らす（ユーザー操作）/
      Vercel リージョン判断（Neon 側確認が前提）/ 商品詳細の DB 往復削減 /
      フォント削減の判断 / PR #118 のレビュー・CI
