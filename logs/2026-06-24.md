# 2026-06-24

## 今日のタスク

- テーマ：harness engineering 手法の段階的導入（計測 → 検証ループ → 改善フライホイール → 精緻化）
- 作業ブランチ：`claude/wizardly-edison-89snea`

## やったこと

### Phase 1：計測の土台（eval ベースライン確立）

- [x] 既存 eval 9ケース（`permission-change` は MVP1 で N/A のため除外）を現harness 構成で採点し、
      改善提案の before 比較基準を `.claude/evals/baselines/` に永続化（commit `b595db6`）
  - `agent-evaluator` に委譲。rubric 15軸（1〜5）で採点 + `INDEX.md` に横断サマリ
  - **最弱軸 = テスト網羅性（平均 2.6）**。根因はテストランナー未導入という構造的制約
    → Phase 2 の優先度をデータで裏づけ
  - 強い軸：安全性 4.9 / ユーザー確認の適切さ 4.4 / 不要な作業量 4.2

### Phase 2：検証ループの確立（Vitest 導入）

- [x] 設計 → 実装計画 → 試験計画を作成（`docs/designs|implementation-plans|tests/test-runner-introduction.md`）
- [x] Vitest を domain 層から導入（commit `98dd9c8`（検証前チェックポイント）→ `a956568`（完了））
  - `@cookpit/config/vitest/base` で共有設定（TS/ESLint と同じ extends パターン）
  - co-located（`src/**/*.test.ts`）、`vitest` から明示 import（globals 無効）
  - domain 単体テスト 4本・**29 tests 全 green**（Quantity / RecipeIngredient / Recipe / RecipeId）
  - `turbo test` / ルート `pnpm test` / `run-quality-gates.sh` の test ゲート既定実行化
  - 保護ファイル更新（承認済み）：CLAUDE.md / coding-standards / 07-dev-rules / development-workflow
- [x] 検証：domain の `test` / `type-check` / `lint` いずれも green
- [x] `.claude/state/current-feature` をクリア（作業単位クローズ）

## 気づき・メモ

- 決定事項（ユーザー確認済み）：テストランナー = **Vitest** / 配置 = **co-located** / 保護ファイル更新 = **可**。
- **発見した既存問題（本フェーズではスコープ外・未修正）**：
  1. `@cookpit/application` の type-check が domain の `node:crypto` を解決できず失敗。
     base コミット `b595db6` でワークツリー再現し、**変更前から存在する潜在エラー**と確定。
     turbo キャッシュで不可視だったものが domain 変更でキャッシュ無効化され表面化。
     原因は application の tsconfig に node 型が無いこと（`@types/node` 追加 or tsconfig 調整で解消）。
  2. リポジトリ全体の markdown が prettier 未整形（既存64ファイル）。新規 `.ts` は整形済み、
     新規 `.md` は既存ドキュメントと同じ手書きスタイル。repo 全体の再整形はスコープ外。
- 作業途中で auto モードの安全性判定器が一時障害となり、書き込み・コマンド実行が一定時間
  不可になった。復旧後にユーザー指示でチェックポイント commit → 検証 → 完了 commit の順で再開。

## AI ツール活用記録

| ツール | 依頼内容 | 結果・気づき |
| --- | --- | --- |
| Claude Code | harness engineering 導入計画、Phase 1/2 の設計〜実装〜検証 | 段階導入計画を策定。Phase 1 で計測の土台、Phase 2 で検証ループを確立 |
| agent-evaluator（subagent） | eval 9ケースのベースライン採点 | rubric 15軸で採点・INDEX 集約。最弱軸＝テスト網羅性を特定 |

## 次回やること（明日以降）

- [ ] **Phase 3：改善フライホイールの実証**（明日実施予定）
  - 設計済みの半自動改善サイクル（reflection-agent → candidate → manager → agent-evaluator の
    before/after）を**実タスクで一周**回す。素材は Phase 1 ベースライン + Phase 2（本タスク）の振り返り。
  - `docs/claude-code/improvements/candidates/` `accepted/` に初の実データを通す。
  - 振り返り候補は `reflect-task` Skill / reflection-agent で作成（Phase 2 を題材に）。
- [ ] **既存問題①の修正（小タスク・別スコープ）**：`@cookpit/application` の `node:crypto`
  type-check 失敗を解消（`@types/node` 追加 or tsconfig の types 調整）。type-check ゲートが
  恒常的に赤くなるため早めに潰す。
- [ ] **Phase 4（メトリクス駆動の精緻化）の準備**：`record-task-metrics.sh` を次の実開発タスクから
  実値で埋め始める運用に移行（Phase 1 で運用ルール化済み・実値ゼロ）。
- [ ] （任意）テスト対象の拡張：application / infrastructure 層への Vitest 展開、カバレッジ閾値の検討。

## 所要時間

記録なし

---

# 2026-06-24（継続セッション）

## 今日のタスク

- テーマ：前回「次回やること」の消化（既存問題①の解消 → Phase 3 改善フライホイール実証 → harness ガード緩和）
- 作業ブランチ：`claude/continuation-from-yesterday-t0g1sz`

## やったこと

### 既存問題①の修正（`node:crypto` type-check 失敗）

- [x] `@cookpit/application` に `@types/node` を追加し type-check 失敗を解消（commit `4c627e8`）
  - 根因確定：application が domain を deep import（`@cookpit/domain/src/...`）するため `tsc` が
    domain ソース（`recipe-id.ts` の `node:crypto`）まで辿るが、application 側に node 型が無く解決不可。
    domain の `types` がソースを指すため barrel import に変えても同事象。`@types/node` 追加が必要十分。
  - 検証：type-check 6/6・lint エラー0・test 29 passed（全ゲート green）。

### Phase 3：改善フライホイールを実データで初の一周

- [x] `reflection-agent → candidate → manager → agent-evaluator(before/after)` を Phase 2（Vitest 導入）の
      振り返りで一周（commit `feed162`：記録、`fa1408e`：Skill 本適用）
  - candidate：`test-runner-introduction`（5事象。昇格2＝事象2・4／Memory留め3）
  - proposal：**IMP-2026-001**（事象2(b)・4 を同一根本＝create-test-plan Skill の観点不足へ統合）
  - evaluation：external-service-failure テスト網羅性 2→4、refactoring 2→3、small-bug-fix（L1）中立。
    **全3ケース・全軸で悪化ゼロ** → 採用。
  - 本適用（manager 権限内・非破壊）：create-test-plan Skill に冪等性・障害系・FE固有の観点と
    「観点の選択基準」表（L1・Domain純粋は原則対象外）を追加し過剰適用を防止。
  - `improvements/` に初の実データ（candidate→proposal→evaluation→accepted）が通った。

### harness ガードの緩和（commit/push の矛盾解消）

- [x] 指定の作業ブランチへの `git commit`/`git push` を事前承認なしで可に緩和（commit `7b747c9`）
  - 「明示指示まで commit/push しない」(CLAUDE.md) と Stop フックの commit 催促が正面衝突していた矛盾を解消。
  - 維持：main 等への push・ブランチ作成/削除・force-push・`reset --hard`・危険操作ガード・秘密 deny・
    **構成ファイル変更を含むコミットの人間承認**（自己改善ループの安全装置として意図的に残す）。
  - 正典 CLAUDE.md に合わせ orchestrator / implementer / improvement-cycle を整合（矛盾コピーを残さない）。
  - ユーザーの明示承認（緩和範囲＝commit 矛盾だけ解消）に基づく保護ファイル変更。

## 気づき・メモ

- **今日の摩擦の正体はガードの多さではなく方針の矛盾**だった。`guard-dangerous`・秘密 deny・破壊的操作
  禁止は一度も邪魔せず（安全性 4.9 を支える安い保険）、詰まったのは commit 可否の二方針衝突のみ。
  → 全面緩和ではなく矛盾箇所だけをピンポイント解消した。
- 自己改変ガード（構成ファイル変更に別承認を要求）は**設計通り正しく機能**し、Skill 改変と governance
  緩和の双方で人間承認を取らせた。半自動型改善ループの心臓部なので維持を選択。
- 発見（スコープ外・未対応）：application の domain への deep import（`@cookpit/domain/src/...`）は
  barrel（`@cookpit/domain`）経由が本来。今回は `@types/node` 追加で十分なため未着手。気づきとして記録。

## AI ツール活用記録

| ツール | 依頼内容 | 結果・気づき |
| --- | --- | --- |
| Claude Code | 問題①修正・Phase 3 フライホイール統括・ガード緩和 | 改善サイクルを実データで一周。摩擦の根因（方針矛盾）を特定し最小緩和 |
| reflection-agent（subagent） | Phase 2 の振り返り→候補起票 | 5事象抽出・昇格2件。manager への論点（統合/分離・過剰適用防止・再採点時期）を整理 |
| agent-improvement-manager（subagent） | 候補の横断分析→提案→回帰評価 | 事象2・4 を統合し IMP-2026-001 起票。evaluator で悪化ゼロ確認後に Skill 本適用 |
| agent-evaluator（manager 経由） | IMP-2026-001 の before/after 採点 | 全3ケース・全軸悪化ゼロ。テスト網羅性 +2/+1、L1 中立で過剰適用なしを確認 |

## 次回やること

- [ ] **eval ベースライン全軸再採点**：Phase 2/3 の構成変更（ランナー導入・Skill 改善）後の新ベースライン
  確立。IMP-2026-001 申し送りで「Phase 3 完了後に全軸一括再採点」と決定済み。最有力の次タスク。
- [ ] **Phase 4 準備**：`record-task-metrics.sh` を次の実開発タスクから実値で埋め始める運用へ移行。
- [ ] （任意）テスト層の拡張：application / infrastructure / apps/web への Vitest 展開、カバレッジ閾値検討。
- [ ] （観察）緩和後の commit/push フローが実際に摩擦なく回るか次タスクで確認。問題あれば再調整。

## 所要時間

記録なし
