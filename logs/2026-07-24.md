# 2026-07-24

## 今日のタスク

昨日（2026-07-23 セッション2）に見積もった改善要望 7 項目を推奨着手順で消化。
作業ブランチ: `claude/resolve-yesterday-issues-gfchu4`。

- 項目4（ボタン配色・L1）/ 項目1（パフォーマンス応急・L1）: 実装・コミット済み
  （詳細は `logs/2026-07-23.md` セッション3 に記録）。
- 項目5（在庫画面から在庫を追加・L2）: 本ログで記録。

## やったこと（項目5: 在庫の手動追加）

L2 のため設計書・実装計画を作成してから着手。

- [x] **L2 成果物**: `docs/designs/pantry-manual-add.md`（確定）/
      `docs/implementation-plans/pantry-manual-add.md` を作成。`docs/04-domain-model.md` の
      Pantry セクションに手動追加の実装追記（全面同期はせず追記のみ）。
- [x] **確定方針**（ユーザー確定）: 商品紐付けは自由入力のみ（`productId=null`、買い物リストの
      `add-item-form` と同一方式）。フォーム項目は必須＝品目名/数量/単位、任意＝保存場所/賞味期限、
      購入日はサーバー現在時刻。
- [x] **api-contract**: `addStockSchema`（displayName min1・amount positive・storedLocation/
      expiresAt nullable）+ `AddStockBody`。契約テスト追加。
- [x] **Application**: `AddStockUseCase` 新設（`find → Pantry.addStock → save → toPantryDto`、
      ConsumeStock と同型）。`AddStockInputDto` 追加。`Stock.create` の例外を
      `InvalidStockOperationError` にラップ。ドメイン層は無変更。単体テスト 5 件追加。
- [x] **Hono ルート**: `POST /api/pantry/stocks`（addStockSchema 検証 → 201 で PantryDto）。
      ルートテスト 3 件追加。
- [x] **Presentation**: `add-stock-form.tsx` 新設（add-item-form 同型の展開パネル）+ `pantry-client.tsx`
      に「在庫を追加」トグル・`handleAddStock` 配線（成功時 stocks 置換＋フォーム閉じ）。
      コンポーネントテスト（add-stock-form 9 件 / pantry-client に PC-18・PC-19）追加。

## 品質ゲート

- **type-check 5/5 PASS** / **lint 0 error**（既存 1 warning `product-form-fields.test.tsx` は無関係）/
  **test 全 PASS**（domain 300 / application 183 / api-contract 212 / infrastructure 52 / web 393）。
- コミットは層で分割（backend 契約+UseCase / web route+UI / docs）。pre-commit・commitlint 通過。

## 気づき・メモ

- **ドメイン変更ゼロで実現**: `Pantry.addStock`（追加＝常に新規 Stock）+ `Stock.create` が既に
  手動追加を支えていた。手動追加は `sourceShoppingItemId=null` で冪等ガード（UNIQUE）の対象外。
- **Drizzle と無関係の往復注意**: `expiresAt` は mapper の `toLocalDateString`（ローカル日付）と
  往復整合させるため UseCase 側で `new Date(\`${expiresAt}T00:00:00\`)`（ローカル 0 時）で構築。
- **雛形の再利用が効いた**: 買い物リストの `add-item-form.tsx` + `shopping-list-client.tsx` の
  `addFormOpen` トグルがほぼそのまま転用でき、L2 だが差分は小さく収まった。
- **未実施**: 実画面確認（manual-browser-verify）は dev サーバー起動が重く未実施。テストは緑。
  ユーザー希望時にスクリーンショットで確認予定。

## 次回やること

- [ ] 項目5 の実画面確認（希望時）。
- [ ] 項目2（買い物リスト・献立の期間指定・L2）。以降 6 → 7 → 3。
- [ ] 項目6 は項目5（手動追加＝在庫を増やす代替導線）完了を前提に着手可能に。
- [ ] フォローアップ（項目1 応急の残り）: 他 3 Repository（pantry/shopping-list/meal-plan）の
      save ループのバッチ INSERT 化、`complete-shopping` の findById 逐次ループ並列化。

## やったこと（項目2: 献立の週選択）

ユーザー確認（AskUserQuestion）で「週を選べる（週単位・スキーマ変更なし）」に確定してから着手。
`docs/designs/meal-plan-week-selection.md`（確定・L2）を作成。

- [x] **Application**: `GetMealPlanByWeekUseCase` 新設（`findByWeek` で指定週取得）。単体テスト 3 件。
- [x] **page.tsx**: `?week=YYYY-MM-DD` を検証（形式・日付妥当性→不正は現在週フォールバック）し、選択週の
      献立を取得。prev/next/current の週識別子を Client へ渡す（Client に Domain を持ち込まない）。
- [x] **meal-plan-client.tsx**: 週送りナビ（前の週/次の週リンク・週ラベル）を追加。作成/空状態の文言を
      選択週で出し分け、作成 POST は選択週で実行。新規 props は任意化し未指定時は現在週選択として従来動作
      （既存テスト後方互換）。週ナビは prev/next 揃い時のみ描画。テスト 3 件追加＋既存 19 件は緑維持。

週モデル（WeekIdentifier・土曜開始）/ DB スキーマ / API 契約は変更なし。

### 品質ゲート（項目2）

- type-check 5/5 / lint 0 error（既存 warning のみ）/ test 全 PASS（application 186・web 396）。
- コミットは feat（application+web）/ docs で分割。

### 気づき・メモ（項目2）

- **「期間指定」は解釈で規模が桁違い**（週選択 L2 ⇄ 日付範囲モデル L3）。CLAUDE.md の設計判断確認ルールに
  従い 1 点だけ確認 → 週選択に確定。ドメイン（`CreateMealPlanUseCase` は任意週対応済み）を活かし
  スキーマ変更ゼロで実現。
- **後方互換の props 設計**: 新 props を任意化し「未指定=現在週」で既存テスト・呼び出しを壊さず、
  週ナビは prev/next 揃い時のみ描画。テスト用 `next/link` モックは aria-label を透過するよう修正。

## やったこと（項目6: 買い物完了時の在庫加算を削除）

ユーザー確認で「在庫加算のみ削除」に確定。`docs/designs/remove-shopping-stock-add.md`（確定・L2）作成。

- [x] **CompleteShoppingUseCase**: `addStocks`/`toAddStockInput` 削除、`pantryRepository` 依存を除去
      （コンストラクタ 4→3 引数）。価格記録・献立 cooking 遷移は維持。
- [x] **価格冪等の再設計**: 在庫ベースのマーカー（`hasStockFromShoppingItem`）喪失を補うため、価格レコード ID を
      買い物品目 ID から決定的に導出し、対象 Product に同 ID があればスキップ。reopen→再完了で二重記録しない
      （スキーマ・ドメイン変更なし）。
- [x] **route**: `POST /:id/complete` の CompleteShopping 構築から `pantryRepository()` 引数を除去。
- [x] **テスト全面更新**: `complete-shopping.use-case.test.ts`（16 件）を在庫加算なし・価格冪等（決定的 ID）
      前提に書き換え。全 PASS。
- 在庫引き算（生成時 `applyPantryDeduction`）は維持。`source_shopping_item_id` 列・
      `hasStockFromShoppingItem` は vestigial として残置（将来クリーンアップ候補）。

### 品質ゲート（項目6）

- type-check 5/5 / lint 0 error / test 全 PASS（application 186・web 396）。

### 気づき・メモ（項目6）

- **冪等マーカーの共有に注意**: 在庫加算と価格記録が「在庫の有無」という単一マーカーを共有していたため、
  在庫加算削除は価格冪等の再設計を強制した。決定的 ID（品目 ID 由来）＋既存スキップで、スキーマ変更なしに
  同等の冪等性を確保。1 品目 = 最大 1 価格レコードの 1:1 対応が前提。

## やったこと（項目7・項目3: 設計のみ・実装は承認後）

L3 2 項目は「設計書/ADR を先に提示」でユーザー確定。実装せず設計成果物のみ作成（レビュー待ち）。

- [x] **項目7**: `ADR-0007-shopping-list-differential-merge.md`（Proposed）＋
      `docs/designs/meal-plan-shopping-sync.md`。明示トリガの同期 UseCase で新規材料のみ追加、
      既存品目（チェック/購入状態）は不変。数量合算・削除追随・自動同期は対象外。集計ロジックは
      generate と共通化。
- [x] **項目3**: `docs/designs/free-text-units.md`（Proposed）。`Unit`→string 化。`isCountableUnit` は
      既知プリセット集合のヒューリスティック、集計/在庫引き算は `normalizeUnit`（trim+NFKC）でマッチ、
      UI は自由入力＋プリセット候補。DB は text のため移行不要。型安全方針撤回のため ADR 化推奨。

## 次回やること（更新2）

- [ ] 項目7・項目3 の設計書/ADR をレビュー → 承認後に実装（各 L3）。
- [ ] 実画面確認（項目5・項目2、希望時）。
- [ ] フォローアップ（項目1 応急の残り：他 Repository のバッチ化・complete-shopping の並列化）、
      vestigial な `source_shopping_item_id`／`hasStockFromShoppingItem` のクリーンアップ。
- [ ] 項目3（単位の完全自由記述化・L3）: Sprint1 確定方針の撤回。型安全性・切り上げ・集計キーの影響設計。
- [ ] 実画面確認（項目5・項目2、希望時）。
- [ ] フォローアップ（項目1 応急の残り：他 Repository のバッチ化・complete-shopping の並列化）。

## 所要時間

1 セッション（項目4/1 の続き → 項目5・項目2 を設計書〜実装〜テスト〜コミットまで）
