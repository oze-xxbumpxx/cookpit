# 2026-05-17

## 今日のタスク

- Sprint：Sprint 1（Recipe ドメイン + CRUD）
- 作業ブランチ：main

## やったこと

- [x] `Unit` 型の最終整理
  - `tsp` / `tbsp` を `小さじ` / `大さじ` に置換（日本語統一方針）
  - `piece` を削除（`個` に統一）
  - `pinch` を削除（`amountNote` で代替済み）
  - 変更後の Unit 型：`g / kg / ml / l / 大さじ / 小さじ / cup / 個 / 本 / 枚 / 玉 / 尾 / 切れ / 束 / 袋 / 缶 / 合`
- [x] Drizzle スキーマ設計・合意
  - `ingredients` / `steps` は JSONB で保持する方針を決定（集約一括取得）
  - `tasks/sprint1-drizzle-schema.md` 作成（Codex 向け実装指針）
- [x] `recipes` テーブルのスキーマ実装（Codex）+ レビュー（Claude Code）+ マイグレーション完了
  - レビューで発見した修正2件：テーブル名 `'recipe'` → `'recipes'`（複数形）、型名 `RecipeNow` → `RecipeRow`（タイポ）
  - `pnpm db:generate` / `pnpm db:migrate` 完了

## 気づき・メモ

- JSONB を選んだ理由：`ingredients` / `steps` は Recipe なしには存在しない「Recipe の一部」であり、常に Recipe ごと読み書きするため JOIN が不要
- Codex はテーブル名を単数形にする傾向がある。指針 MD に「複数形」と明示した方が良い
- 型名のタイポ（`RecipeNow`）は指針 MD の `RecipeRow` と一致していなかった。Codex は指示書の型名を確認せずに命名することがある

## AI ツール活用記録

| ツール      | 依頼内容                           | 結果・気づき                                                   |
| ----------- | ---------------------------------- | -------------------------------------------------------------- |
| Claude Code | Unit 型整理・スキーマ設計・MD 作成 | 問題なし                                                       |
| Codex       | recipes テーブルのスキーマ実装     | テーブル名単数形・型名タイポの2件を Claude Code レビューで発見 |

## 次回やること

- [ ] `DrizzleRecipeRepository` 実装指針 MD 作成
- [ ] `DrizzleRecipeRepository` 実装（Codex）+ レビュー
- [ ] UseCase 実装（Create / Get / Update / Delete）
- [ ] Hono API エンドポイント
- [ ] 画面実装

## 所要時間

記録なし
