# 2026-07-27

## 今日のタスク

- Sprint：Sprint 2
- 作業ブランチ：`claude/sugar-unit-store-dropdown-bdv2v6`
- 変更レベル：**L3**（新規 API 2 本 + Repository インターフェース拡張。DB スキーマ変更なし）
- feature-name：`store-delete-and-unit-price-basis`

実運用で出た 3 件の不都合をまとめて解消する。

1. 砂糖を 1kg で買うのに単価が「100g あたり」で出て読みづらい
2. 内容量に `3001kg` と誤入力した価格記録が本番データに残っており、直す手段が無い
3. 店舗プルダウンに不要な店舗が残っており、削除する手段が無い

## やったこと

- [x] 調査：3 件の原因を切り分け
  - ①は `UnitPriceCalculator` が 100g / 100ml 基準へ正準化して保存し、
    `unitPriceBasisLabel` が `kg` でも `'100g'` を返していたため
  - ②は**コードのバグではなくデータ**。`parseQuantity('3001kg')` は仕様どおり
    値 3001・単位 kg を返す。価格記録を消す/直す手段が UI・API のどちらにも無いのが問題
  - ③は `StoreRepository` に `delete` が無く、ルートも `GET` / `POST` の 2 本だけ
- [x] ユーザーへ 3 論点を確認（表示基準 / 修正手段 / 参照がある店舗の扱い）
- [x] L3 成果物：要件定義書・設計書・ADR-0012・実装計画・試験計画・レビュー記録
- [x] ①単価表示：`formatUnitPrice()` を追加し、**保存値は据え置きで表示だけ換算**
- [x] ②価格記録の削除：Domain → Application → ルート → 画面まで縦に実装
- [x] ③店舗削除：参照件数を数えて 1 件でもあれば 422（ADR-0012）
- [x] 品質ゲート：lint / type-check / test すべて PASS（1,482 件）

## 気づき・メモ

- **保存値を変えずに表示だけ換算する**のが今回の肝。`unit_price_amount` は店舗間比較の
  正準値なので、ここを単位ごとに変えると 300g の記録と 1kg の記録が比較できなくなる。
  「ユーザーの違和感」は表示の問題であって、計算の問題ではなかった。
- **表示側の単位判定を正規化してはいけない。** `UnitPriceCalculator` は
  `new Set(['g','kg'])` の厳密一致で正準化しており、表示側だけ `toLowerCase()` を足すと
  `'KG'` で「正準値は 1 単位あたり・表示は 1kg 基準」になり**単価が 10 倍ズレる**。
  同じ厳密一致に揃え、理由をコードコメントと PF-06b の試験で固定した。
  正準化と表示は**必ず対で読む**。
- **`formatUnitPrice` の導入で `unitPriceBasisLabel` が死んだ**。金額とラベルは常に対で
  出るため、置き換え後に本番の呼び出し元がゼロになる。テストだけが参照する公開関数を
  残しかけたので削除し、試験は `formatUnitPrice` 経由へ移した。
  **関数を足したら、置き換え元が死んでいないか必ず確認する。**
- **削除の設計は「何が失われるか」で決まる。** ADR-0011（買い物品目）は無条件の物理削除、
  ADR-0012（店舗）は参照があれば拒否。差は「消えるものを自分で入れ直せるか」で、
  価格履歴は日をまたいで貯めるので入れ直せない。同じ「削除」でも判断を揃えるべきではない。
- **Base UI の `Select.Item` に削除ボタンは入れられない。** `role="option"` の内側に
  `<button>` を置くのは ARIA 的に不正で、項目クリックの選択挙動とも競合する。
  ユーザー要望は「プルダウンで削除したい」だったが、**プルダウン直下の既存パネル**
  （元々店舗追加があった場所）を管理パネルに拡張する形で満たした。制約は先に伝えて合意を取った。
- **Hono RPC の型は共通 `onError` 由来の 404 / 422 を知らない。** `response.status !== 404` が
  型エラーになる。`shopping-list-client.tsx` に既存の対処（`const status: number = ...`）が
  あったのでそれに揃えた。**同じ壁に当たったら回避策を発明する前に既存例を探す。**
- **「最近の記録」に内容量を出す**のは要求に明示されていないが、
  どの行が `3001kg` かを画面で見分けられないと削除機能が使えないため入れた。
  スコープ外変更はこの 1 点のみで、レビュー記録に理由を残した。
- リポジトリインターフェースへのメソッド追加で、テスト用 InMemory 実装 6 箇所が
  コンパイルエラーになる。**手順の最初にわざと型エラーを出して波及範囲を確定させる**のが速い。

## AI ツール活用記録

| ツール      | 依頼内容                                       | 結果・気づき                                                                 |
| ----------- | ---------------------------------------------- | ---------------------------------------------------------------------------- |
| Claude Code | 3 件の不都合の調査・設計・実装・テスト・文書化 | 原因切り分けで②が「バグではなくデータ」と判明し、対応方針（削除機能の追加）が変わった |
| Codex       | —                                              | 今回は委譲なし                                                               |
| Gemini      | —                                              | —                                                                            |
| Perplexity  | —                                              | —                                                                            |

## 次回やること

- [ ] **実画面確認**（今回 BLOCKED）。dev サーバーを立てて 2 点を見る
      1. 記録行を 3 カラム化＋内容量表示したので、モバイル幅 390px で崩れないか
      2. 店舗管理パネルが店舗数の増加で縦に伸びすぎないか
- [ ] **本番データの復旧**（ユーザー操作）。商品詳細で `3001kg` の記録を削除 → `1kg` で記録し直す →
      店舗管理パネルから不要な店舗を削除
- [ ] （提案）店舗のリネーム（`PUT /api/stores/:id`）。名前を打ち間違えたまま価格を記録すると、
      参照ありで削除できず直す手段も無い（ADR-0012 で受容したトレードオフ）
- [ ] （提案）価格記録の編集。今回は「削除して記録し直す」で足りるとユーザーが選択
- [ ] 前回からの継続：PR #118 のレビュー・CI、products フォームへの `useLeaveConfirmation` 適用、
      作り方の手順（steps）の並べ替え

## 所要時間

約 2 時間（調査 30 分 / 設計・文書 40 分 / 実装・テスト 40 分 / ゲート・レビュー 10 分）
