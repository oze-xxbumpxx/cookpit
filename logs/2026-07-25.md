# 2026-07-25

## 今日のタスク

- Sprint：MVP1 継続（機能追加ではなく構造リファクタリング）
- 作業ブランチ：`claude/project-refactoring-vc1pmz`

プロジェクト構成・実装コードのリファクタリングを**観点分け**で実施。棚卸しで 8 観点を抽出し、
ユーザー確定で A〜G の 7 観点を対象（H = docs の棚卸しは対象外）。挙動不変を要件とし、
観点ごとに 1 コミット＋品質ゲートで進行。

## やったこと

- [x] **棚卸し（Level 0）**: 実測ベースで 8 観点を提示。deep import 285 箇所・onError 11 連 if・
      日付ヘルパ 5 重複・shopping-list-client 446 行/useState 16 個・1000 行超テスト 2 件 ほか。
- [x] **L2 成果物**（`71c0c6c`）: `docs/designs/project-refactoring.md`（confirmed）/
      `docs/implementation-plans/project-refactoring.md` / `docs/tests/project-refactoring.md` /
      `docs/decisions/ADR-0010-package-public-boundary.md`（Accepted）。
- [x] **観点A: パッケージ公開境界**（`f529b52`）: `packages/domain/src/index.ts` にバレルを整備し、
      `@cookpit/domain/src/...` の deep import 285 箇所を `@cookpit/domain` へ統一（70 ファイル）。
      衝突する `recipe-ingredient.ts` の `interface ProductId` を `ProductRef` へ改名。
      infrastructure バレルに `export * as schema` を追加し `pglite-client.ts` の直参照を解消。
- [x] **観点C: エラー変換**（`671802a`）: 422 系の共通基底 `InvalidOperationError` を新設し
      `InvalidStateError` をその派生に。基底を継承していなかった `InvalidStockOperationError` も
      寄せて、`onError` を 11 連 if → 3 分岐へ。`export default app` を named export に変更。
- [x] **観点F: 日付ヘルパ**（`75f1ad6`）: infrastructure 3 リポジトリの `toDate`/`toDateString` を
      `repositories/mappers.ts` へ、application 2 mapper の `toLocalDateString` を
      `shared/date.ts` へ集約。domain（week-identifier）は独立維持。
- [x] **観点E: UseCase 重複**（`ebbf896`）: `load-shopping-list.ts` に
      `loadActiveShoppingList` / `requireItem` / `findUpdatedItem` を抽出し、
      mark-as-bought / set-item-checked / reassign-store の重複を解消。`=== undefined` を `?? null` へ。
- [x] **観点B: DI 統一**（`b355c0f`）: Server Component 10 ページの `new DrizzleXxxRepository(getDb())`
      を `@/server/repositories` のファクトリへ統一。`@cookpit/infrastructure` の import を
      `server/repositories.ts` と `db/*` に閉じた。
- [x] **観点D: UI 共通化**（`4eb9ec2`）: `lib/use-api-action.ts` を新設し 4 client・14 ハンドラの
      定型処理を集約。shopping-list-client は 446 → 393 行、useState 16 → 8 個。
      既存コンポーネントテスト 439 件は**無修正で PASS**。フック単体テスト 7 件を追加。
- [x] **観点G: テスト分割**（`5c247ed`）: 1171 行 → 8 ファイル（application）、1160 行 → 4 ファイル
      （web client）、814 行 → 6 ファイル（route）。フィクスチャは test-helpers / test-fixtures へ集約。
      it のタイトル・アサーションは変更なし（移動のみ）。

## 気づき・メモ

- **`main` が指す index.ts が空だった**のが観点A の根本原因。`main`/`types` は
  `./src/index.ts` を宣言済みだったので、バレルを埋めるだけで規約が揃った。
  application / infrastructure / api-contract は既にバレルを持っており、domain だけ例外だった。
- **機械置換の正規表現で隣接 import を巻き込んだ**（1 回目の失敗）。
  `\{([\s\S]*?)\}` は貪欲でなくても `}` を跨いで後方の `from '@cookpit/domain/src/...'` に
  マッチしてしまい、直前の `import { describe, it } from 'vitest'` を飲み込んだ。
  波括弧内は `[^}]` に限定すること。type-check が即座に検出したので影響は無し。
- **設計時の前提が 1 件外れた**: 「11 個のエラーは全て 2 基底のいずれかを継承」と書いたが、
  `InvalidStockOperationError` だけ素の `Error` 継承だった。実装前に全件確認する手順を
  実装計画に入れていたため着手時に判明。基底の付け替えでは**メッセージ書式を固定しない
  中間基底**（`InvalidOperationError`）を挟むことで、自由記述メッセージを保ったまま統合できた。
- **テストモックは本物より貧しい**: `useApiAction` が成功時に常に `response.json()` を呼ぶ設計に
  したところ、meal-plan のテストモックが `json()` を持たず「通信エラー」に落ちた。
  本文を使わない成功時のための `onSuccessWithoutBody` を分けて解決。
  「使わない本文をパースしない」のは実装としても正しい。
- **Hono の ClientResponse はステータスごとのユニオン**で、成功側だけ `ok: true` を持つ。
  共通フックで型安全を保つには `Extract<TResponse, { ok: true }>` で成功側を取り出す必要がある。
- **楽観的更新は共通化しない**判断が正解だった。`useOptimistic` + `startTransition` は
  状態更新の順序そのものが挙動（transition 内の通常 setState は保留される）なので、
  フックに寄せると「操作中 item のみ disable」が壊れる。共通化対象は非 optimistic に限定。

## AI ツール活用記録

| ツール      | 依頼内容                                         | 結果・気づき                                                                 |
| ----------- | ------------------------------------------------ | ---------------------------------------------------------------------------- |
| Claude Code | 観点分けリファクタリング（棚卸し〜実装〜テスト） | 7 観点を 7 コミットで完了。各段で品質ゲート通過。機械置換は正規表現に注意     |
| Codex       | 未使用                                           | 記録なし                                                                     |
| Gemini      | 未使用                                           | 記録なし                                                                     |
| Perplexity  | 未使用                                           | 記録なし                                                                     |

## やったこと（レビューと S1 修正）

- [x] **レビュー**（`0d3da03`）: reviewer サブエージェントの独立レビュー＋機械検証を
      `docs/reviews/project-refactoring.md` に統合。**Must 指摘なし・差し戻し不要**。
      機械検証は it 表題・`expect()` 数・`not.toHaveBeenCalled` 数の完全一致（観点G）、
      エラー→HTTP 変換の等価性（観点C・使い捨てテスト）、バレル網羅 64/64（観点A）、
      `pnpm build` 成功（17 ルート不変）。
- [x] **S1 修正**（ユーザー指示）: `useApiAction` の実行中フラグを単一キーから
      `ReadonlySet<string>` へ変更。共有インスタンスの別操作が並行しても実行中フラグを
      奪い合わなくなり、変更前の独立した `refreshing` / `addSubmitting` / `submittingStockId`
      と等価になった。公開 API から `pendingKey` を削除し `pending` / `isPending(key)` に統一。
      `pantry-client` の行 disable をグループ内 `isPending` 解決に変更（N1 も解消）。
      回帰テスト UAA-08 を追加（web 446 → 447）。
- [x] **S2 は受容**: 非 silent の再取得でエラーバナーが開始時に消える差分は記録のみ。

### 気づき・メモ（レビュー）

- **ローカル `main` ref が origin/main より古く、最初の機械検証が偽陽性だらけになった**。
  `git show main:<file>` で比較すると、実際には存在しない差分（テスト表題の不一致・
  8 件の消失）が大量に出た。分岐元は merge-base（`71c0c6c^` = `590dfc2`）を明示指定すべき。
- **回帰テストは「旧実装で落ちること」まで確認する**。UAA-08 を追加したあと、
  `git stash` で単一キー実装に戻して実際に失敗することを確認した。
  これをやらないと、指摘を検出しないテストを「回帰テスト」と呼んでしまう。
- **レビュー指摘も検証してから受け入れる**。reviewer の「手動更新中は focus による
  silent 再取得がガードでスキップされ得る」は、`useEffect([])` が第 1 レンダーの
  クロージャを捕捉するため成立しない（見える `pendingKey` は常に初期値）。
  レビュー記録に訂正として明記した。

## 次回やること

- [ ] （任意）実画面確認（manual-browser-verify）。観点 B / D は画面に触れるため、
      希望があれば dev サーバー + Playwright で確認する。テストは全て緑。
- [ ] （提案・別タスク）`RecipeIngredient.productRef` を構造的 `interface ProductRef` から
      `ProductId` 値オブジェクトへ変更する（ADR-0010 案 D）。集約をまたぐ ID 参照の
      公称型付けが回復する。mapper・repository・テストのリテラルに波及するため独立タスク。
- [ ] （提案・別タスク）観点 H（`docs/tasks/sprint1-*` 14 件と `docs/sprints/` の archive 化）。
- [ ] （残置）recipe / product のフォーム系 client 7 ファイルは 400 のフィールドエラー解析を
      伴うため `useApiAction` の対象外。共通化するなら別設計が要る。
- [ ] （残置）`source_shopping_item_id` 列の物理削除（破壊的スキーマ変更。7/24 から継続）。

## 所要時間

1 セッション（棚卸し → 設計・計画・ADR → 観点 A/C/F/E/B/D/G の実装とテスト → コミット 8 本）
