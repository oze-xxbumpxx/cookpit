# 2026-08-09

**Sprint 8 キックオフ + Unit A（stock-edit）— 設計から実装レビュー・実画面確認まで**

## 今日のタスク

- Sprint：**Sprint 8**（在庫の実用化）キックオフ + Unit A の設計一巡
- 作業ブランチ：`claude/sprint8-design-r6qn4z`
- 変更レベル：**L3** / feature-name `stock-edit` / 実装ルート **Codex 委譲**
- **実装は行わない**（設計フェーズのみ）

## やったこと

- [x] Explore 3 本並行で現状を実測（Pantry バックエンド / UI / PWA・通知基盤）
- [x] ユーザー確定 4 点: ユニット分割（Unit A/B/C）/ アラート方式（Web Push + 日次 Cron）/
      今回の設計範囲（Unit A のみ）/ 実装ルート（Unit A = Codex）
- [x] roadmap に Sprint 8 のユニット分割表を追記（`05c9f2a`）
- [x] 要件定義書 + 設計書（`5c30b9a`）→ P-1〜P-6 をユーザー確認 → 確定内容へ更新（`0d2a100`）
- [x] ADR-0016（`75ff72b`）
- [x] 契約設計書（`87d71ec`）/ 試験計画・約 100 観点（`1172d27`）/ 実装計画 12 ステップ（`0af31bb`）
- [x] Codex 実装指示書 8 本 + レビュー（`619dd32`）→ Should 反映（`2b7ca39`）
- [x] 文書レビュー: **Must 3 / Should 7 / Nice 9**。Must 3 件と Should 3 件を反映

## 設計で確定した内容

| # | 確定 | 備考 |
| --- | --- | --- |
| P-1 | 編集対象は 期限・保存場所 **+ 数量** | 推奨案（期限・保存場所のみ）から変更 |
| P-2 | `PUT /api/pantry/stocks/:stockId`・全項目必須・`null` でクリア | ADR-0015 の PATCH 却下と同じ理由 |
| P-3 | 完了パネルは行ごと折りたたみ／任意展開 | Q-1 の「縦に長くなる」懸念への対処 |
| P-4 | `/pantry` カードに保存場所ラベル **+ 緊急度チップの両方** | 推奨案（ラベルのみ）から変更 |
| P-6 | `Stock.updateDetails(props)` 一括 | `Product.updatePriceRecord` と同型 |

## 気づき・メモ

- **設計書は下流工程で 8 件の欠陥が見つかった。** 確定済み（ステータス confirmed）でも
  下流が実コードを当たると誤りが出る。内訳は契約設計で 2 件・試験計画で 4 件・レビューで
  Must 3 件（一部重複）。**特に「Zod バリデーション失敗は 422」は明確な事実誤り**で、
  実際は `@hono/zod-validator` が `app.onError` を経由せず自前で 400 を返す。
  → **設計書の記述を下流 Agent に「前提」として渡すときは、実測を指示しないと誤りが伝播する。**
- **P-1 の変更（数量の追加）が罠のもう 1 つの顔を炙り出した。** `onConflictDoUpdate.set` は
  `amountValue` はあるが **`amountUnit` が無い**。期限・保存場所が丸ごと無い元の罠より、
  「値は変わるのに単位だけ変わらない」ほうが**動いて見えるぶん気づきにくい**。
  推奨案どおり期限・保存場所だけにしていたら、この顔は見つからなかった。
- **既存テストが罠を「正しい挙動」として固定していた。** `同一 id の再 save() は amountValue
  のみ更新し不変フィールドを維持する` が `amountUnit === '個'` / `expiresAt === '2026-07-18'` /
  `storedLocation === 'fridge'` をアサートしている。修正すると 3 アサーションが落ちる。
  **これを実装計画に書いておかないと、実装者は「回帰した」と誤診する。**
- **レビュー M-1 が false PASS の構造そのものだった。** 緊急度チップの表示条件で
  「期限切れ（残日数が負）」の扱いが未定義で、下限を付ける実装でも **RTL 観点に overdue が
  無いため全件 Green のまま手動確認だけが落ちる**。累計 3 回起きている型と同じ構造を、
  今度は**実装前に**捕まえられた。SR-EDIT-07 を必須ケースに追加した。
- **上限を明示した Agent 委譲が 4 本連続で機能した。** contract-designer 16 呼び出し /
  test-designer 30 / implementation-planner 32 / reviewer 25。いずれも成果物あり。
  前々タスクの「上限なし reviewer が 46 呼び出しで成果物ゼロ」とは対照的。
- **調査結果をプロンプトに埋め込むと再探索が減る。** Explore 3 本の実測結果を
  architecture-designer のプロンプトに全部貼ったので、設計 Agent は 20 呼び出しで済んだ。
- **セッションが usage 上限で 1 度中断した。** contract-designer の実行中。
  成果物は都度コミット・push していたので失われたのは 1 エージェントの実行分だけだった。

## AI ツール活用記録

| ツール | 依頼内容 | 結果・気づき |
| --- | --- | --- |
| Claude Code | Sprint 8 キックオフ + Unit A の L3 設計一巡（実装なし） | 上限明示の委譲が 4 本連続で機能。設計書の事実誤りを下流 3 工程が独立に検出した |

## 次回やること

- [ ] **PR #145 のマージ**（Sprint 8 Unit A 一式）。CI green を確認済みならマージする
- [ ] **MB-11〜14（買い物完了パネル）の実画面確認** — dev シードに献立・買い物リストが
      無く BLOCKED。**Sprint 8 完了条件「買い物完了時に賞味期限を入力できる」に直接対応する
      経路**で、今回 2 件の欠陥がどちらも RTL を通過した実績を踏まえると実画面で見る価値が高い
- [ ] **`docs/tasks/codex/stock-edit/README.md` の「Codex モデル / reasoning effort」行の記入**
      （IMP-2026-025 の効果をモデル条件込みで比較する記録経路。空欄のまま）
- [ ] 設計レビューの **Nice 9 件・未対応 Should 4 件**の扱いを決める（`docs/reviews/stock-edit.md`）
- [ ] **Unit B（expiry-alert）の設計** — Web Push + 日次 Cron の方向は確定済み。
      `push_subscriptions` テーブル・VAPID 鍵・`vercel.json` はいずれも未整備
- [ ] Sprint 8 完了条件 2 件（買い物完了時の任意入力 / 既存在庫の後付け編集）を roadmap で
      チェック済みにするか判断する（Unit A マージ後）
- [ ] `docs/07-dev-rules.md` §環境の既知の事実 への追記（前回からの持ち越し + 今回分）:
      `next build` 後に `next dev` するなら `.next` を消す / dev 起動中に PGlite を再シード
      しない / **PGlite の dev DB は abort すると壊れるので作り直す** /
      **`pkill -f "next dev"` は自分のシェルを巻き込むのでポートか PID で落とす**
- [ ] 振り返り候補の manager 横断分析。**実画面確認の false PASS 系が累計 4 回目**
      （今回は「リロードしてからアサートしたため画面反映の欠陥を通した」形）

## 所要時間

約 4 時間（調査 Explore 3 本 20 分 / ユーザー確認 3 往復 15 分 / 要件・設計 25 分 /
ADR 15 分 / 契約・試験計画（並行）20 分 / 実装計画 15 分 / Codex ブリーフ 8 本 25 分 /
受け入れレビュー 25 分 / 実画面確認 40 分 / 欠陥 2 件の修正 30 分 / PR・記録 20 分。
Subagent の実行待ちと usage 上限による中断は含まない）
