# 2026-08-13

## やったこと

**Sprint 9 を開始。タスク 1（オフライン書き込みキュー）を L3 で一巡し、実装・自動テストまで完了。
タスク 2（Function リージョン）の下調べで、当初の前提が誤っていたことを実測で確定した。**

### Sprint 9 タスク 1: オフライン書き込みキュー（offline-write-queue）

`docs/designs/shopping-list-screens.md` S-5 で「実装しない・別フェーズ候補」として明示的に外していた
書き込みキューを実装した。完了条件「オフライン中のチェック操作が復帰後に反映される」に直結する。

#### 方式を roadmap の記述から変更した

roadmap のタスク名は「PWA オフライン書き込みキュー（**Background Sync**）」だったが、
Background Sync API（`SyncManager`）は Chromium 限定で **iOS Safari が未対応**と見られる。
Cookpit の主対象は iPhone の PWA なので、名指しされた技術では完了条件を満たせない。

**ユーザー確定で案 C（アプリ層のみ）を採用**した。IndexedDB に永続キューを持ち、
`online` イベント・フォーカス復帰・マウントで再送する。`sw.ts` には一切触らない
（Sprint 8 で踏んだ `self.__SW_MANIFEST` の terser インライン化の罠を再度踏まないため）。

**この iOS 非対応は一次情報で裏取りできていない。** この環境では `developer.mozilla.org` /
`vercel.com` への egress がポリシーで遮断されている。設計書に「未確認の前提」として明記した。

#### キュー対象をチェック操作のみに絞った根拠

対象 API の冪等性を調べたところ、はっきり二分された。

| API | 送るもの | 再送安全性 |
| --- | --- | --- |
| `POST /items/:itemId/checked` | 絶対値 | 安全（last-write-wins） |
| `POST /items/:itemId/bought` | 絶対値 | 安全 |
| `DELETE /items/:itemId` | — | 安全（404 を成功扱い済み） |
| `POST /stocks/:stockId/consume` | **差分** | **再送で二重消費** |

完了条件はチェック操作だけを求めており、既に冪等な API で満たせる。`consume` をキューに載せるなら
Sprint 9 タスク 3（冪等性キー）が設計上の先行条件になる。

#### 成果物 5 点

| 成果物 | 規模 |
| --- | --- |
| `docs/requirements/offline-write-queue.md` | 約 215 行 |
| `docs/designs/offline-write-queue.md` | 約 665 行（P-1〜P-9） |
| `docs/tests/offline-write-queue.md` | 約 364 行（観点 54 件） |
| `docs/implementation-plans/offline-write-queue.md` | 約 1974 行（6 ステップ） |
| `docs/reviews/offline-write-queue.md` | 約 222 行（Must 2 / Should 5 / Nice 3） |

#### 実装（6 ステップ・4 コミット）

| 区切り | 内容 | コミット |
| --- | --- | --- |
| Step 1-2 | IndexedDB ラッパー + React フック + 依存追加（`idb` / `fake-indexeddb`） | `d6d6ee9` |
| Step 3 | `shopping-list-client.tsx` への配線（P-2 / P-4 / P-5） | `0c4a07a` |
| Step 4-5 | 未送信インジケーター + roadmap 表記修正 | `f05b005` |
| Step 6 | 最終確認（スコープ検査） | — |

**最終ゲート**: lint 0 errors、type-check PASS、テスト **2042 件 PASS**
（domain 436 / application 353 / api-contract 293 / infrastructure 100 / web 860）。
`sw.ts` と `packages/` 配下に差分が無いことを明示的に確認済み。

## 気づき

### 下流 Agent が上流の欠陥を検出する構造が 3 タスク連続で再現した

| 検出者 | 内容 |
| --- | --- |
| test-designer | `useCheckedSyncQueue` がマウント時に無条件で IndexedDB を開くため、happy-dom で `ReferenceError` となり**無関係な既存 dom テストまで壊れる** |
| test-designer | 設計書はキュー利用可否のガードを要求するが、公開インターフェースに判別手段が無い（**設計内の矛盾**） |
| reviewer | `LC-22` が flush 先行化で必ず落ちる。`fake-indexeddb` を実際にインストールして**実測した上で**の指摘 |
| implementer | 実装計画のサンプルコードの不備 4 件（`OQ-03` 欠落・`vi.clearAllMocks` 漏れ・`UOQ-10` のタイミング制御・TTL 境界の片側欠落） |

reviewer の `LC-22` の指摘が質的に良い。「壊れるかもしれない」ではなく、依存パッケージを実際に入れて
**`IDBRequest` はマイクロタスクではなくタスクで解決する**ことを確かめた上で「`act()` の 1 回の
マイクロタスク消化では到達しない」と結論している。この知見は Step 2・Step 3 の両方で再利用された。

### 非決定的なテストが実装の欠陥を隠していた

`LCQ-09` が「1 回のはずが 3 回」で失敗し、`LCQ-EX` は実行ごとに成否が変わった。原因は
**アンマウント後も `flush()` が走り続け、前のテストの送信が次のテストへ混入**していたこと。
`afterEach` の `cleanup()` と `vi.clearAllMocks()` では止まらない（IndexedDB の解決がマクロタスクで
起きるため）。フックにアンマウント検知を足して解消し、3 回連続実行で安定を確認した。

設計・計画のどちらも**「flush の生存期間」を規定していなかった**。`flushingRef` による二重発火の
排他はあったが、コンポーネントの寿命とキュー処理の寿命の関係が未定義だった。

### implementer の停止でデバッグコードが production に残った

Step 3 の implementer がセッション上限で停止した（累計 4 回目）。成果物はディスクに残っていたが、
**デバッグ用の `console.log` が production コードに 2 箇所、テストに 1 箇所残ったまま**だった。
`eslint-disable-next-line no-console` が添えられていたため **lint では検出できなかった**。
Orchestrator が引き継いで除去し、失敗していたテスト 2 件も原因特定して修正した。

「停止時の成果物はディスクに残る」は前回の学びどおりだったが、**残った成果物が完了形かどうかを
判定する手段が無い**という弱点が、今回は実害として現れた。

## Sprint 9 タスク 2: 前提が誤っていたことを実測で確定した

roadmap のタスク 2 は「Function だけ hnd1 へ移すと DB 往復が悪化する」という懸念を前提にしていた。
これは**Function と Neon が同居している**ことを暗黙の前提にしていたが、実測すると成立していなかった。

### 測定方法

DB を触る API と触らない API の応答時間を比べる。差分がそのまま DB 往復のレイテンシになる。
`GET /api/stores` は `findAll()` の**単一クエリ = 1 往復**であることをコードで確認済み。

```
DBなし (/api/push/vapid-public-key): 0.274 / 0.132 秒
DBあり (/api/stores):                0.449 / 0.353 秒
差分 = DB 1 往復:                    0.175 / 0.221 秒
```

**Function ↔ Neon の 1 往復に 0.18〜0.22 秒。**「同居」の想定値（数 ms）の 50 倍以上で、
既に地球の反対側にある。

### チェック操作への影響

チェック操作 1 回は DB を**逐次 4 往復**する（`findById` 1 回 + `save()` 3 回。
`drizzle-shopping-list.repository.ts:44` / `:71` / `:83` / `:99`）。`neon-http` はクエリごとに
独立した HTTPS リクエストを投げるため、往復回数がそのままレイテンシに掛かる。

```
0.2 秒 × 4 往復 + 0.13 秒（利用者→Function） ≒ 0.9 秒
```

**店内でチェックを 1 つ入れるたびに約 0.9 秒。** Sprint 9 のゴール後半「体感速度の実測値が改善する」は、
オフラインキューではなくこの構成が原因だった。

### 結論と残件

「Function だけ移すと悪化する」という懸念は**当たらない**。移設は改善手段になる。ただし
*どちらへ寄せるか* は Neon の場所次第で、**Neon のリージョンは未確認のまま**（ダッシュボードで
確認できず、この環境からは egress 遮断で調べられない）。

`save()` が 3 往復している点（チェック 1 件のために全品目を delete して再 upsert する構造）は
リージョンとは別の増幅要因。Sprint 7 から「非トランザクションで並行書き込みが価格記録を静かに消す」
という申し送りも付いている箇所なので、**Sprint 10 のトランザクション導入と同時に扱うのが筋**。
今回はスコープ外として記録に留めた。

## 次回やること

- [ ] **タスク 1 の実機確認** — 自動テストは 2042 件通っているが、実際のブラウザでオフラインにして
      チェック → 復帰 → 反映、を確認していない。`docs/tests/offline-write-queue.md` の MB ケース 5 件。
      **完了条件「オフライン中のチェック操作が復帰後に反映される」はこれが済むまで未達扱い**
- [ ] **タスク 2 の決着** — Neon のリージョンを確認するか、作業ブランチの `vercel.json` に
      `"regions": ["hnd1"]` を足して Preview で A/B 測定する（1 行追加・本番は無変更）。
      差分が縮めば Neon はアジアなので hnd1 へ移して確定、広がれば米国なので据え置き
- [ ] **タスク 3（`ConsumeStock` の二重送信抑止）** — 未着手。キュー対象を `consume` へ広げる場合の
      先行条件でもある
- [ ] **タスク 4（フォント削減）** — 未着手。見た目の判断が要るため保留中
- [ ] **Background Sync の iOS 対応の裏取り** — 方式選択の根拠だが未確認。覆っても案 C は無駄にならない
- [ ] **改善候補の昇格判断**（`docs/claude-code/improvements/candidates/offline-write-queue.md`）:
      (1) 名指しされた外部 API の対応状況を設計着手前に一次情報で確認する工程（2 回観測）
      (2) 委譲プロンプトの定型 3 点（3 タスク連続で観測）
      (3) Agent 停止時の `console.log` / `DEBUG` / `.only` 混入検査（4 回観測・今回初めて実害）
- [ ] 前日からの持ち越し: Sprint 8 の本番環境変数 4 点 / Unit B の実機確認（Sprint 10）/
      `pnpm.overrides` の範囲見直し / `docs/07-dev-rules.md` §環境の既知の事実 への追記

## 所要時間

設計フェーズ 約 1.5 時間（調査とユーザー確認 30 分 / 要件・設計 30 分 / 試験計画・実装計画（並行）
20 分 / レビュー 10 分）。実装フェーズ 約 1 時間（Step 1-2 25 分 / Step 3 の引き継ぎと修正 20 分 /
Step 4-6 15 分）。タスク 2 の下調べ 約 20 分。Subagent の実行待ちと usage 上限による中断は含まない。

---

## セッション2（Cursor Cloud Agent）

- Sprint：9
- 作業ブランチ：`claude/sprint9-execution-81spn0`

### やったこと

- [x] **Background Sync の iOS 対応を一次情報で確認**し、案 C の根拠を確定した。
      iOS Safari は非対応。出典:
      [MDN BCD SyncManager](https://github.com/mdn/browser-compat-data/blob/main/api/SyncManager.json)
      （`safari` / `safari_ios` とも `version_added: false`）、
      [caniuse background-sync](https://caniuse.com/background-sync)
      （Safari on iOS 3.2–26.5 / Safari TP とも Not supported）、
      [WebKit standards-positions #14](https://github.com/WebKit/standards-positions/issues/14)
      （open、`concerns: privacy` / `concerns: power`、肯定的ポジションなし）。
      更新: `docs/{designs,requirements,reviews}/offline-write-queue.md`
- [x] **Neon / Function リージョンを特定**した。
      Neon = `ap-southeast-1`（シンガポール、`logs/sprint-0.md`）。
      本番 Function = `iad1`（`x-vercel-id: pdx1::iad1::...`、2026-08-13 再実測）。
      roadmap の「同居しているから Function だけ移すと悪化する」前提は不成立。
- [x] 本番ベースラインを再測（この環境から `https://cookpit-web.vercel.app`）。
      `/api/push/vapid-public-key` は `VAPID_PUBLIC_KEY` 未設定で 500 だが DB は触らない
      （Sprint 8 持ち越しの本番環境変数）。warm で約 0.15 秒。
      `/api/stores` warm で約 0.35〜0.38 秒。差分 ≒ DB 1 往復 **約 0.20 秒**（前回と一致）。
- [x] Preview A/B 用に `vercel.json` へ `"regions": ["hnd1"]` を追加（本番 `main` は無変更）。
      記録: `docs/05-roadmap.md` タスク 2 注記、`docs/07-dev-rules.md` §環境の既知の事実。
      PR: https://github.com/oze-xxbumpxx/cookpit/pull/158 （draft）
      Preview: https://cookpit-web-git-claude-sprint9-ex-7c7964-oze-xxbumpxxs-projects.vercel.app
      **測定は Vercel SSO（Deployment Protection）で BLOCKED**。`/api/stores` が
      `https://vercel.com/login` へ 302 する。Function まで到達できないため `x-vercel-id` の
      `hnd1` 確認も未了。本番は保護なしで測れた。
- [x] Neon Console で Region を目視確認。`AWS Asia Pacific 1 (Singapore)`。
      Function の本番行き先を **`sin1`** に確定し、`vercel.json` を `hnd1` → `sin1` へ差し替えた。
- [x] タスク 3（`ConsumeStock` 冪等性）とタスク 4（フォント削減）を Sprint 10 へ繰り越し
      （roadmap 更新）。完了条件の「フォント転送量」は本 Sprint から外した。
- [x] オフラインキューの Chromium 代替確認（`dev:pglite` + Playwright）。
      `apps/web/scripts/verify-offline-write-queue.mjs`
      MB-01 PASS / MB-04 PASS / MB-02・03・05 BLOCKED（理由つき）/ FAIL 0。
      iPhone PWA 実機はマージ後の確認事項として残す。

### 気づき

- Neon と同リージョンの Vercel は **`sin1`（シンガポール / `ap-southeast-1`）**。
  チェック操作は DB を逐次 4 往復するため、利用者に近い `hnd1` より DB 同居の `sin1` を採った。
  概算（日本の利用者・チェック 1 回 = 利用者→Function + DB 4 往復）:
  - 現状 `iad1`: 約 0.15 + 4×0.20 ≒ **0.95 秒**（実測と一致）
  - `hnd1`（東京）: 約 0.3–0.4 秒
  - `sin1`（同居）: 約 0.15–0.25 秒
  本番の実測はマージ後。Preview は SSO でエージェントから測れない。
- `/api/health` は `SELECT 1` するため「DB なし」プローブには使えない。
- Preview の Deployment Protection がオンだと、エージェントからの curl では A/B できない。
- Playwright でチェックすると `aria-label` が「をチェックする」→「のチェックを外す」に変わる。
  オフライン後のアサーションは品目名だけの role locator にしないと、状態確認でタイムアウトする。

### 次回やること

- [x] **PR #158 をマージ**して本番 Function を `sin1` にする（ユーザー実施。マージ 2026-08-13T02:00:14Z）
- [x] 本番のチェック体感確認（ユーザー実施・2026-08-13 OK）。Chromium で MB-01 代替済。
      機内モードでの再送は明示確認なし（試験計画では MB は完了のブロッカーではない）
- [ ] Sprint 10: `ConsumeStock` 冪等性 / フォント削減 / トランザクション
- [ ] **改善候補の昇格判断**（`docs/claude-code/improvements/candidates/offline-write-queue.md`）
- [ ] 持ち越し: Sprint 8 の本番環境変数 4 点（`VAPID_PUBLIC_KEY` 未設定で vapid が 500）/
      Unit B の実機確認（Sprint 10）/ `pnpm.overrides` の範囲見直し

### 所要時間

記録なし（Cloud Agent セッション）

---

## セッション3（マージ後の本番測定）

- Sprint：9
- 作業ブランチ：`cursor/sprint9-sin1-measurement-d3ac`（測定記録のみ）

### やったこと

- [x] PR #158 マージ後の本番を測定。`x-vercel-id: pdx1::sin1::...` で Function が
      **sin1** になったことを確認。
- [x] 同じプローブで before/after を比較（この VM から `https://cookpit-web.vercel.app`）。

  | 指標 | マージ前 `iad1` | マージ後 `sin1` |
  | --- | --- | --- |
  | DB なし（vapid、warm 中央値） | 約 0.13〜0.15 秒 | 約 0.25 秒 |
  | DB 1 往復（`/api/stores`、warm 中央値） | 約 0.35〜0.38 秒 | 約 0.25 秒 |
  | 差分 = Function↔Neon | **約 0.20 秒** | **測定誤差（≈ 0 秒）** |

  vapid が遅く見えるのは、測定元が日本ではなく Vercel の `pdx1`（米国西部）寄りのため。
  利用者→Function ではなく **Function↔Neon** が今回の改善対象。同居後は stores と vapid が
  同じ 0.25 秒に重なった。チェック 1 回（DB 4 往復）の増幅は消えた。
  日本の利用者→`sin1` は米国東部より近いので、店内の体感はさらに短い想定。
- [x] ユーザーが本番の買い物リストでチェック操作を確認し OK（2026-08-13）。
      体感の本丸である `POST .../checked` が実機で問題ないことを確認した。

### 次回やること

- [x] iPhone / 本番でのチェック体感確認（ユーザー実施。OK）
- [ ] Sprint 10 の繰り越し 3 件（`ConsumeStock` / フォント / トランザクション）
- [ ] 本番 `VAPID_PUBLIC_KEY`（vapid は引き続き 500）

### 所要時間

記録なし（Cloud Agent セッション）

---

## セッション4（Sprint 9 クローズ判断）

- Sprint：9
- 作業ブランチ：`cursor/sprint9-close-9e92`
- 変更レベル：**L0→L1**（調査で終了可否を判断し、roadmap にクローズ節を追記）

### やったこと

- [x] ユーザーの「sprint9は終了でよい？」に対し、Sprint 7 と同じ基準（完了条件を実コード・
      実測で裏取りする）で判断した。**締めてよい。**
- [x] `docs/05-roadmap.md` に Sprint 9 クローズ節を追加し、MVP2 スケジュール表を
      「クローズ済み（2026-08-13）」へ更新した。

### 気づき

Sprint 8 の完了条件 3 件目（実機 Push）は未達のまま残し、Sprint 9 の MB-01（iPhone 機内モード）は
試験計画がブロッカーではないと明示しているので完了印を付けた。同じ「実機未確認」でも、
試験計画が完了条件に含めるかを先に決めてあるかどうかで扱いが分かれる。

### 次回やること

- [x] Sprint 10 着手（セッション5でキックオフ）
- [ ] 改善候補の昇格判断（`docs/claude-code/improvements/candidates/offline-write-queue.md`）
- [ ] 持ち越し: 本番 `VAPID_PUBLIC_KEY` / Unit B 実機確認 / `pnpm.overrides` の範囲見直し

### 所要時間

記録なし（Cloud Agent セッション）

---

## セッション5（Sprint 10 キックオフ）

- Sprint：**10**（献立の精度 + 横断基盤）
- 作業ブランチ：`cursor/sprint10-kickoff-7bfc`
- 変更レベル：**L1**（roadmap / ログのみ。プロダクションコードは変更しない）
- 実装ルート：対象外（キックオフ。Unit A の設計は Gate A の後）

### 今日のタスク

Sprint 10 を開始する。ユニット分割・変更レベル・実装ルートを宣言し、実装は行わない。

### やったこと

- [x] ブリーフィング（前回セッション4の「Sprint 10 着手」が今回の入力）
- [x] 変更レベル分類（classify-change）と実装ルート宣言。7 タスクを完了条件との対応で分割
- [x] `docs/05-roadmap.md` に Sprint 10 の進め方（ユニット表・Gate A 3 問・Unit B の前提崩し）を追記
- [x] タスク 2 の「潜在バグ」を実測。**JST で前日ずれが再現する**ことを確認した
- [x] タスク 5 の先行条件を実測。本番 `GET /api/push/vapid-public-key` は **500**
      （`{"error":"Server misconfigured"}`）。VAPID 未設定のまま

### ユニット分割とレベル

| ユニット | タスク | レベル | 理由 | 実装ルート |
| --- | --- | --- | --- | --- |
| Unit A: meal-plan-sync | 1 | L3 | ADR-0007 の「既存品目は不変」を覆す。境界例は上位レベル | Orchestrator（設計判断が残るので Codex にしない） |
| Unit B: uow | 3 | L3 | アーキテクチャ変更。全 Repository 横断。ドライバ変更が先行条件 | Orchestrator |
| Unit C: consume-idempotency | 6 | L2〜L3 | 契約変更。DB に冪等キーを持つかで確定。完了条件外 | 設計後に Codex 可 |
| サイド: mapper-jst | 2 | L1 | 既存 `toLocalDateString` への置換。仕様を変えないバグ修正 | メイン直接 |
| サイド: expiry-alert 実機 | 5 | L0 | コード変更なし。ユーザー実機 | ユーザー |
| サイド: small-ux / フォント | 4 / 7 | L2 / L0 | 既存画面のみ / 見た目の判断。完了条件外 | 余力 / ユーザー |

省略する工程: キックオフ時点では設計書・要件・ADR・実装計画を作らない
（「先回りして全スプリント分の設計書を書かない」）。Unit A の成果物は Gate A の後。

### タスク 2 の実測

`packages/application/src/meal-plan/meal-plan.mapper.ts` の
`scheduledDate?.toISOString().slice(0, 10)` に対し、Infrastructure は
`toLocalDate('YYYY-MM-DD')` = `new Date('YYYY-MM-DDT00:00:00')`（ローカル 0 時）で復元する。

| TZ | `new Date('2026-07-11T00:00:00').toISOString().slice(0,10)` | `toLocalDateString` |
| --- | --- | --- |
| `Asia/Tokyo` | **`2026-07-10`**（前日） | `2026-07-11` |
| `UTC`（Vercel 本番） | `2026-07-11`（ずれない） | `2026-07-11` |

shopping-list / pantry の mapper は既に `toLocalDateString` を使っている。meal-plan だけ取り残し。
本番 UTC では隠れ、日本のローカル dev と「日付だけ持つ値をローカル 0 時で持つ」経路で顕在化する。
修正は L1 の独立 PR でよく、Unit A を待たない。

### Unit A の Gate A（次セッションの入力）

推奨つきで 3 問。詳細は `docs/05-roadmap.md` Sprint 10 進め方。

1. 削除対象は `from_meal_plan` × `pending` のみか
2. 数量は `pending`/`checked` を新集計値で上書きし、`bought` は触らないか
3. トリガは明示ボタンのままか

### 気づき・メモ

- **neon-http は transaction を持たない。** Unit B の「トランザクションを入れる」は
  ドライバ変更（`neon-serverless` の `Pool`）なしには成立しない。Sprint 9 の
  「チェック 1 回 = HTTPS 4 往復」と同じ構造。キックオフで見つけたので、
  Unit B 着手時に「transaction() を足すだけ」と設計しないで済む。
- **タスク 5 のブロッカーはコードではなく本番環境変数。** 購読 UI は VAPID が無いと
  ボタン無効で degrade する設計なので、未設定のまま他機能は壊れない。
- ブリーフィングスクリプトはログ先頭の「次回やること」（セッション1）を拾うため、
  同日複数セッションがあると最新セッションの申し送りを見落とす。今回はログを直接読んで回避した。

### AI ツール活用記録

| ツール | 依頼内容 | 結果・気づき |
| --- | --- | --- |
| Cursor Cloud Agent | Sprint 10 キックオフ（分類・ユニット分割・実測） | 実装なし。Gate A 3 問と neon-http の前提崩しを roadmap に残した |

### 次回やること

- [ ] **Unit A の Gate A 3 問を確定**してから設計書を書く（実装は自動では始めない）
- [ ] タスク 2 の L1 修正（`meal-plan.mapper.ts` を `toLocalDateString` へ。独立 PR）
- [ ] 本番 `VAPID_PUBLIC_KEY` を入れて再デプロイ → タスク 5 の実機確認を再開できる状態にする
- [ ] 改善候補の昇格判断（`docs/claude-code/improvements/candidates/offline-write-queue.md`）
- [ ] 持ち越し: `pnpm.overrides` の範囲見直し

### 所要時間

記録なし（Cloud Agent セッション）

## セッション6（Sprint 10 Unit A: meal-plan-sync）

- Sprint：**10**
- 作業ブランチ：`cursor/sprint10-kickoff-7bfc`
- 変更レベル：**L3**（ADR-0007 の「既存品目は不変」を覆す。境界例は上位レベル）
- 実装ルート：Orchestrator（implementer が設計を読んで直接実装。Codex 委譲しない）
- feature: `meal-plan-sync`

### 今日のタスク

Gate A の推奨案をユーザーが採用したので、Unit A（献立同期の削除追随・数量上書き）を
要件〜実装まで進める。

### やったこと

- [x] Gate A 確定（推奨案採用・2026-08-13）
      1. 削除は `from_meal_plan` × `pending` のみ。`bought` / 手動追加は残す
      2. `pending` の数量は新集計値で上書き。`bought` は触らない
      3. 明示 sync ボタン維持。自動同期しない
      4. Pantry 巻き戻しはしない
- [x] 成果物: `docs/requirements/meal-plan-sync.md` /
      `docs/designs/meal-plan-sync.md` /
      `docs/decisions/ADR-0018-meal-plan-sync-delete-and-quantity.md` /
      `docs/implementation-plans/meal-plan-sync.md` /
      `docs/tests/meal-plan-sync.md`
- [x] Domain: `ShoppingItem.updateRequiredAmount` /
      `ShoppingList.updateItemRequiredAmount`
- [x] Application: `SyncShoppingListFromMealPlanUseCase` を追加・更新・削除の 3 系統に拡張。
      数量増加分のみ `applyPantryDeduction`
- [x] Presentation: `diffSyncResult` / `describeSyncResult`（追加N件・更新N件・削除N件）
- [x] Infrastructure プロダクションコードは変更なし（upsert が既に数量列を含む。R-5）
- [x] 対象パッケージの lint / type-check / test は通過（web lint の既存 warning 1 件はスコープ外）

### 気づき・メモ

- ADR-0009 により画面のチェックは `bought`。Gate A の「checked」は実装では bought として扱い、
  削除も数量更新もしない。
- 手動削除した `from_meal_plan` の pending は再同期で復活する（R-2。確認ダイアログの文言どおり）。
- ShoppingList 保存と Pantry 保存の部分失敗窓は Unit B（UoW）まで残す（R-3）。
- `pnpm --filter … exec vitest` は `vitest` が見つからない。`pnpm --filter @cookpit/domain test` を使う。

### AI ツール活用記録

| ツール | 依頼内容 | 結果・気づき |
| --- | --- | --- |
| Cursor Cloud Agent | Gate A 採用後の Unit A 設計・実装 | 契約・DB は変えず既存 `POST /sync` の意味を拡張した |

### 次回やること

- [ ] 本 PR の人間レビュー（Gate B）とマージ判断
- [ ] Unit B: UoW / `neon-serverless` ドライバ変更の Gate A
- [ ] タスク 2 の L1 修正（`meal-plan.mapper.ts` を `toLocalDateString` へ。独立 PR）
- [ ] 本番 `VAPID_PUBLIC_KEY` を入れて再デプロイ → タスク 5 の実機確認
- [ ] 改善候補の昇格判断（`docs/claude-code/improvements/candidates/offline-write-queue.md`）
- [ ] 持ち越し: `pnpm.overrides` の範囲見直し

### 所要時間

記録なし（Cloud Agent セッション）

## セッション7（Sprint 10 サイド: mapper-jst）

- Sprint：10
- 作業ブランチ：`cursor/mapper-jst-c4d2`
- 変更レベル：L1（既存日付整形のバグ修正。契約・DB なし）
- 実装ルート：メイン直接

### やったこと

- [x] `toPlannedRecipeDto` の `scheduledDate` を `toISOString().slice(0,10)` から `toLocalDateString` へ置換
- [x] mapper テスト 3 件（null / ローカル暦日 / UTC 日付との差）
- [x] shopping-list-core の潜在バグ注記を「Sprint 10 タスク 2 で解消」へ更新
- [x] Unit A / B を roadmap 上で完了（PR #162 / #164 マージ済み）

### 次回やること

- [ ] 本 PR のマージ
- [ ] 本番 `VAPID_PUBLIC_KEY` を入れて再デプロイ → タスク 5 の実機確認（Sprint 10 完了条件 3 件目）
- [ ] 余力: Unit C（ConsumeStock 冪等）/ 小粒 UX / フォント判断
- [ ] 持ち越し: `pnpm.overrides` の範囲見直し
